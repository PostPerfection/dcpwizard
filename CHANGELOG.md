# Changelog

## Unreleased

### Added
- **The `kdm` subcommand has a CLI test**: `kdm_cli.rs` runs the command over a generated signer chain and recipient certificate, checks the file it writes carries the SMPTE ST 430-1 namespace and the CPL id and content title it was given, verifies the signature with xmlsec1, and unwraps the KDM with the recipient's private key to get back one 16-byte content key under the key id the document lists.
- **`kdm-email` and the drive copy's free-space check run in tests**: `kdm_email_reaches_the_smtp_server` starts an SMTP listener, sends through `send_kdms` with `Security::None`, and finds the recipient, the subject and the zip in what arrived. `refuses_a_dcp_larger_than_the_free_space` hands `copy_to_drive` a sparse 1 EiB picture file and checks nothing is copied, and the check itself is asserted to name the destination and the shortfall.

### Removed
- **`kdm --format` and `kdm-batch --format` are gone**: every KDM is a SMPTE ST 430-1 ETM now, which is what DCP-o-matic writes for an Interop DCP as well. The `interop` setting produced a SMPTE ETM with its KDMRequiredExtensions renamed into the digicine namespace while it kept the SMPTE MessageType and the SMPTE-only elements, so it was not the digicine 2004 document real Interop gear reads. Reading is unchanged: `decrypt --kdm` takes the layout from the decrypted block length, so a 134-byte legacy KDM still unwraps.

### Changed
- **`run-gpu-gui.py` launches the GPU GUI on Linux, macOS and Windows**: it had a hard-coded `lib64/libgrokj2k_plugin.so` and `LD_LIBRARY_PATH`, so a macOS grok install in `lib/` with the dylib and `grok_kernels.metallib` was refused before the build started. The script now picks the plugin name, library directory and loader variable from the OS, requires the Metal kernels next to the dylib, and copies the sidecar itself on Windows where the bash helper does not run.
- **An encrypted package with no signer is refused before the encode**: `create --encrypt` without `--signer-cert` wrote a package dcpdoctor reports `dcp_not_signed` over twice, and a KDM binds to a signed CPL, so the result was an encrypted DCP no projector could be given keys for. `CreatePlan` carries whether a signer was named, and the pre-build check both front ends share refuses the pair, naming `--signer-cert` and `--signer-key`. The GUI panel has nowhere to name a signer and never set one, so its encrypt checkbox was refused at submit until its stored `signingCert` and `signingKey` were wired into the job, covered in Fixed below. `prebuild_refusals.rs` asserts the refusal writes nothing under `--output` and that the same job with a generated chain still packages.
- **Sound is widened to the DCI depth, or refused before the encode**: the sound file was wrapped as it stood, so `create --audio` over a 16-bit WAV packaged a DCP whose sound essence failed the verification `create` now runs, after the whole picture had been encoded. `prepare_packaged_channels`, the pass that already reorders 5.1 and fills a track to its packaged width in one streamed rewrite, now widens 8 and 16-bit samples to the 24-bit the essence carries as it goes: a shift into the top bytes, with 8-bit's unsigned offset flipped first, so nothing is resampled and no second ffmpeg pass runs. Both front ends get it, since the pass sits inside `create_dcp`. What cannot be widened without losing samples is refused at the pre-build check that `--check` reports and both front ends share, naming the file, whether it carries 32-bit integer or float, and `ffmpeg -c:a pcm_s24le`. A rate other than 48000 or 96000 Hz is refused there too: the frame-alignment refusal that was already there only catches a rate that is not a whole number of samples per frame, so 44.1 kHz at 25 fps passed it and reached the packager. `prebuild_refusals.rs` builds a DCP from a 16-bit master and reads 24 back off the sound descriptor with dcpdoctor clean, and asserts the float, 32-bit and 44.1 kHz refusals write nothing under `--output`.
- **The CLI archive runs where it is unpacked**: the Linux and macOS tarballs held the binary alone, which links `libgrokj2k` at load time, so an unpacked download died with `error while loading shared libraries: libgrokj2k.so.1` unless the user already had a grok install and pointed `LD_LIBRARY_PATH` at it, which the README told them to do. Both archives now carry the library in `lib/` beside the binary, copied from the grok install `Setup grok` leaves at `grok-install/lib64` or `grok-install/lib`, and the binary looks for it there: `$ORIGIN/lib` through patchelf on Linux, `@loader_path/lib` through `install_name_tool` on macOS, where the load command is rewritten to `@rpath` and the binary and the library are re-signed ad hoc. The release workflow unpacks the archive it just built and runs `--version` from it with `LD_LIBRARY_PATH`, `DYLD_LIBRARY_PATH` and `DYLD_FALLBACK_LIBRARY_PATH` unset, so a missing library fails the build rather than the download. The Windows zip already shipped `grokj2k.dll` next to the exe and is now run the same way.
- **Every package `create` writes is verified before the command returns**: the landing page says a DCP is verified before it goes out, and `create` ran nothing over the package it had just written, so a broken one was found by whoever ran `dcpwizard verify` next, or at the cinema. `create` now runs `verify_dcp_with_options` over `--output` once the scratch is cleared and before any `--upload-to-tms`, prints the findings through the same `print_verify_findings` the `verify` command uses, and exits 1 on an error with the package left where it is. `--no-verify` skips it and says `--no-verify: the finished package was not verified` on stderr. `--check` still returns before anything is written, so it verifies nothing. The GUI's job option defaulted to off where its own checkbox was ticked, so a job submitted without the field went out unverified: `verify_after_build` defaults it to on in `pipeline.rs` and the front end sends the checkbox's own state rather than falling back to off.
- **96, 100 and 120 fps are refused instead of packaged**: `create --frame-rate` and the GUI's frame rate menus offered the DCI HFR addendum's three highest 2K rates, and a package built at one of them carried a composition edit rate no packaging standard covers, so dcpdoctor reported `cpl_invalid_edit_rate` over every CPL. ST 429-2 §8.1 gives a composition Edit Rate the values 24, 25, 30, 48, 50 and 60 only, and ST 429-13 stops at 60 as well. The three rates are gone from `hfr::FrameRate`, from `is_valid_frame_rate` and from both GUI menus, and `--frame-rate 96`, `100` or `120` is now refused by name before any encode, naming ST 429-2 and the 60 fps cap. Legal SMPTE rates are 24, 25, 30, 48, 50 and 60, Interop stays 24, 25, 30 and 48, and 4K stays capped at 30 fps. The CLI test that used to assert the `cpl_invalid_edit_rate` now packages a 2K DCP at 48, 50 and 60 fps and verifies each with zero errors.
- **`serve` reads the whole request and takes an API key**: the REST API was a hand-rolled parser doing one 8 KiB `read` per connection, so a `POST /create` whose `DcpConfig` JSON was larger than that, or arrived in two TCP segments, reached the handler truncated and came back `400 Invalid config`. It is built on postkit's `RestServer` now, which reads the request line, the headers and exactly `Content-Length` bytes of body, refuses a body over 1 MiB with a 413, and gives every connection a read and a write timeout. `serve --api-key <key>` requires that key in `X-Api-Key` or `Authorization: Bearer` on every route but `GET /health`, compared without stopping at the first differing byte, and answers 401 before routing, so an unknown path tells an unauthorized caller nothing; without the flag the API stays open to anyone who can reach the bind address, as it was. The routes and their JSON are unchanged (`GET /health`, `GET /daemon-status`, `GET /jobs`, `POST /create`, `POST /verify`, `GET /metrics` as `text/plain; version=0.0.4`), unknown fields in a posted `DcpConfig` are still ignored, and every job route still proxies to the job daemon and answers 503 without it, now naming `dcpwizard daemon` in the message. The server had no tests at all; it has them over real sockets now, a create job posted through the daemon that ends with the DCP on disk and the verify route passing over it, and a 9 KB config body the old parser could not have read whole.
- **`create --hdr-dci` authors a DCI HDR Addendum DCP from an HDR master**: the flag only accepted a source that already carried PQ X'Y'Z' (`--hdr-already-pq`) or a user LUT (`--hdr-to-dci-lut`), so an HDR10, HLG, PQ P3-D65 or Dolby Vision grade had no path into a DCI HDR package. postkit's `HdrDcdmTransform` now converts the master itself: `--hdr-source <hdr10|hlg|pq-p3d65|dolby-vision>` names the grade, or the master's own colour tags do, and `--hdr-peak-nits` names the luminance the BT.2390 roll-off starts from, defaulting to the master's MaxCLL, then its mastering display maximum, then 1000 cd/m² with a warning. A Dolby Vision master's RPU is read in process for its profile and MaxCLL, profile 8.1 transforms as its HDR10 base layer, and profile 5 is refused by name. An SDR or untagged master is refused naming `--hdr-source`, and `--allow-generic-hdr-tonemap` is now refused with `--hdr-dci`, since the tone map lands on SDR. The decode converts the master's YCbCr with zscale at its own matrix and range, so a white patch at 299.6 cd/m² reads back as the addendum's own 2524/2546/2583, where swscale's conversion landed 8 codes below it. The GUI's HDR panel takes the same grade and peak luminance. A `--burn-subtitle` or a `--watermark` over an HDR master is refused before the encode, naming the PQ-encoded HDR samples the draw would land in, and both frontends now read one `frames_not_display_rgb` predicate for that: the GUI derived it from `applies_xyz_transform`, which also refused a P3 or Rec.2020 burn the CLI and postkit both allow.
- **The DCI HDR signalling is what the addendum names**: the picture descriptor carried a P3-D65 `ColorPrimaries` UL the addendum names no item for, and the CPL claimed no EOTF at all. The descriptor now carries only the ST 2084 `TransferCharacteristic` UL, the composition metadata carries an ST 429-16 `ExtensionMetadata` in the `http://www.dcimovies.com/schemas/2018/HDR-Metadata` scope with `Image Encoding Parameters` / `EOTF` / `ST 2084`, and the ISDCF content title takes the `HDR1` modifier the registry ties to that extension, sorted after the frame rate.
- **`watch` builds a DCP from a master that lands in the folder**: it used to poll for folders that already held an ASSETMAP and post a `dcp.detected` webhook that called the package complete, and nothing dropped into the folder was ever encoded. It takes `--output` and any `create` flags after `--`, waits for a dropped video file or frame folder to stop changing (same size, modified time and entry count across two polls, `--interval` seconds apart), runs `create` on it with the file stem as the title and a same named `.wav` and `.srt` beside it as sound and subtitle, writes the job log to `<output>/<stem>.log` beside the package, moves the source and its sidecars into `done/` or `failed/` under a numeric suffix where the name is taken, and posts `dcp.created` or `dcp.failed`. Each package is built by a `dcpwizard create` child process, one at a time, so a source `create` refuses ends that job rather than the watcher.
- **`watermark` marks a DCP's picture essence**: the command took a video or image file and drew the mark with ffmpeg drawtext, which nothing a DCP is made of passes through. It takes a DCP directory now and writes a marked DCP: every picture frame is decoded, the mark is composited over the picture and over anything already burnt into it, and the frame is re-encoded, at the source picture's own average bandwidth (its frame lengths summed at its edit rate, read without decoding them) unless `--video-bit-rate` names one. Sound, subtitle, caption and Atmos tracks ship unchanged under their own asset ids, `--kdm`/`--recipient-key`/`--keys` mark an encrypted source into a cleartext output, and a stereoscopic source is refused rather than marked in one eye. `--font-size` is a percent of the frame height, as the burn-in flags take theirs, `--colour` is RRGGBB or RRGGBBAA and `--font` names the face; `--video-codec` and `--crf` are gone with the movie-file path, and so is `watermark::embed_watermark`. `create --watermark TEXT` marks at build time through the same builder, with `--watermark-font-size`, `--watermark-colour` and `--watermark-position`, drawn over a `--burn-subtitle` where both are given and refused loud on a source that reaches the encoder as X'Y'Z' or as codestreams. postkit's `SourcePreparation` carries the mark beside the subtitle burn and composites it after them, on a packed 16-bit frame or on the planar components an existing codestream decodes to.

### Fixed
- **Grok preview plays the DCP's sound track**: a JPEG 2000 package took grok for the picture and stopped mpv, so a composition with a MainSound MXF was silent. postkit now resolves MainSound with the picture, reads the PCM and plays a stereo downmix on the picture clock.
- **The desktop GUI paints its page on macOS**: a production `dcpwizard-gui` still compiled as a Tauri dev build, so the webview loaded `http://localhost:1421` and, with Vite not running, stayed a blank white window. `custom-protocol` is a default feature now, which is what Tauri's template uses so `tauri build` serves the bundled frontend and `tauri dev` turns the feature off. The terminal-guard fork that restores the TTY after WebKitGTK exits is a no-op off linux: WKWebView in a forked process never commits a page load either.
- **The embedded preview on macOS is the right way up**: the GL view is a sibling of WKWebView, so it is layer-backed (`_NSOpenGLViewBackingLayer`) and Core Animation composites it with a top-left origin. `FLIP_Y` was left off as if this were a classic bottom-left `NSOpenGLView`, which put the picture's top at the bottom of the panel. It matches linux's GtkGLArea now.
- **The GUI signs an encrypted package it builds**: the panel stored a signing certificate and key in its settings and never sent them into a create job, so every GUI build set `signer: None` and the plan's `signed: false`, and with the shared pre-build check now refusing an encrypted package that names no signer, the encrypt checkbox failed at submit. `submit_job` carries `signingCert`, `signingKey` and `signingChain` into `JobConfig`, `job_signer` builds a `PackageSigner` only when both cert and key are present and non-empty, treating the panel's empty-string default as no signer, and `build_dcp_config` and `job_plan` take it, so an encrypted GUI job signs its CPL and PKL and a bad cert or key fails at `check_usable` before anything is written. `pipeline.rs` tests assert a named signer makes the plan `signed` and the config carry the cert, key and chain, that an empty or missing cert or key names no signer, and that a wired signer over a generated chain passes `check_usable` end to end.
- **`create` builds a DCP from an image sequence**: `--video` pointing at a folder of stills was read as a folder of codestreams, so the job encoded nothing and died at packaging with `J2K input directory contains no codestreams`, having written nothing. The only route from DPX, TIFF, EXR, PNG, JPEG or BMP to a package was `encode` first and `create` over its `j2k` folder second. A folder postkit's `detect_input_type` calls an image sequence is now compressed through `encode_image_sequence`, the same call the `encode` command makes, at `--video-bit-rate` or the DCI cap, and the codestreams it writes are what the packager wraps, so `--trim-start`/`--trim-end` cut them like any other codestream source. The concat list postkit writes beside them joins the scratch `remove_intermediates` clears, so no `frames.ffconcat` is left inside the package. `image_sequences.rs` builds a DCP from a three frame 2048x1080 sequence in each of the six formats the README names and decodes every packaged frame back out.
- **`hdr10-inject` names the right primary for each colour**: the command filled `Hdr10Metadata` with the P3-D65 chromaticities rotated one place, so the ST 2086 SEI it wrote gave red the green primary (13250, 34500), green the blue one and blue the red one, and any tone mapper reading the mastering display saw a gamut no display has. The three pairs sit in one `P3D65_MASTERING_DISPLAY` constant now, red (34000, 16000), green (13250, 34500) and blue (7500, 3000) over ST 2086's 0.00002 steps, which is the display postkit's own `x265_hdr10_params` test spells out. `hdr_metadata.rs` injects into a PQ tagged source and asserts ffprobe reads all six chromaticities, the white point, both luminances and MaxCLL/MaxFALL back off the encoded stream.
- **`accessibility --standard` checks the tracks each instrument names**: the EAA and Ofcom lists required an HI mix channel and named no caption track at all, and Ofcom required sign-language video, so a DCP with a 5.1 bed plus HI and VI-N passed `--standard eaa` while a captioned DCP without an HI mix failed it. The EAA names its access services in Annex I Section IV(b)(ii) and Article 3(6) as subtitles for the deaf and hard of hearing, audio description, spoken subtitles and sign language interpretation, and names no audio mix anywhere (https://eur-lex.europa.eu/legal-content/EN/TXT/PDF/?uri=CELEX%3A32019L0882). Ofcom's Code on Television Access Services names subtitling, audio description and signing, quotas of 80 to 90, 10 and 5 per cent of a schedule under Communications Act 2003 s303, and no mix (https://www.legislation.gov.uk/ukpga/2003/21/section/303). Ontario's Integrated Accessibility Standards Regulation reaches internet websites and web content only, and its s.14(4) schedule exempts WCAG success criteria 1.2.5 Audio Descriptions (Pre-recorded) from the Level AA duty while 1.2.2 Captions (Prerecorded) is Level A and not exempt (https://www.ontario.ca/laws/regulation/110191). `--standard eaa` and `--standard ofcom` now require a closed-caption asset and a VI-N narration channel and `--standard aoda` requires the caption asset alone, the HI mix is a recommendation under all three, and sign language is a recommendation under the EAA and Ofcom. None of the three instruments names a cinema deliverable, so every list stays dcpwizard's own mapping of the access services an instrument names onto DCP tracks rather than a citation, and the README spells each list out. The CVAA list is unchanged and was not re-read against its own instrument. A CLI test per standard builds two real DCPs, one with a closed-caption asset and HI and VI-N channels and one with neither, and asserts the first passes and the second names the standard and each missing track at the severity the standard gives it.
- **`export` writes a Rec.709 screener**: the command passed the picture MXF's `xyz12le` frames to the encoder with no colour options, so swscale converted them under its unspecified-means-BT.601 matrix and the file named no primaries, transfer, matrix or range. Read as Rec.709, which is how a player reads an HD file, a flat 0x5a8f3c master came back 82.04/125.53/53.19 against the 85.17/134.97/55.53 the DCP's own picture decodes to, 7.0 percent off on green. Every format now runs `scale=out_color_matrix=bt709:out_range=tv` and tags the result bt709 and tv, which puts the same frame at 85.12/134.93/55.47, inside 0.1 percent of the picture. The pixel format is the one the codec is for: ProRes 422 HQ carries 4:2:2 10-bit where it carried the 4:4:4 its `apch` tag does not cover, H.264 and H.265 carry 8-bit 4:2:0 High where they carried High 4:4:4 Predictive 10-bit that a browser or QuickTime will not open, and DNxHR HQ carries 4:2:2 8-bit. ProRes and DNxHR take `pcm_s24le` sound, which is what a master for approval carries, where every format took AAC. `export_dcp` returns ffmpeg's own error text now instead of -1, so the command prints `export failed: ffmpeg could not export to out.mp4: ...` and the job queue records that in place of `exporting the DCP failed with code -1`.
- **The GUI leaves no scratch inside the DCP**: a GUI job wrote `j2k`, `j2k_trimmed`, `audio_work`, `right`, `hdr_tonemap.mov` and `slvs_sound.wav` into the output directory and left every one of them sitting beside the ASSETMAP the package ships, where `create` removed its own. Both front ends call `dcpwizard_core::intermediates::remove_intermediates` now, one list of the names a build writes inside the output directory, so the CLI and the GUI cannot drift. Both remove them only once the package is written: `create` used to clear the codestreams and the `.dcpwizard-encode.json` resume state whatever the packaging returned, so a `--resume` after a failed package re-encoded every frame. A failed package now keeps both and says `the encoded frames are still in <dir>, --resume reuses them`. The shared list also covers `j2k_still` and `hdr_to_dci_source.mov`, which `create` wrote and left behind, and a codestream directory handed in as the source is never removed.
- **`accessibility --standard` refuses a name it does not know**: the flag was a plain string matched against `eaa`, `aoda` and `ofcom` with everything else falling through to CVAA, so `--standard nonsense` ran the CVAA check and printed `Standard: Cvaa` as though that had been asked for. It is a value enum now, so clap refuses an unknown name and lists `cvaa`, `eaa`, `aoda` and `ofcom`.
- **`dv-inject` and `hdr10-inject` check their inputs before running the tool**: a missing input, a missing RPU and a missing `dovi_tool` all came back as `Failed to run dovi_tool: No such file or directory (os error 2)`, which names none of the three. `dv-inject` now names the input or the RPU file it cannot find, and says `dovi_tool is not installed or not on PATH, get it from https://github.com/quietvoid/dovi_tool` when the tool is missing; `hdr10-inject` names a missing input the same way.
- **The `watermark` command's CPL carries the hashes of what it ships**: the marked package's reel assets were written with no `<Hash>` and its PKL with an empty `AnnotationText`, so dcpdoctor reported `cpl_missing_hash` for the picture and the sound and `pkl_annotation_text_mismatch`, while `create --watermark` on the same source reported neither. Every reel asset now repeats the base64 SHA-1 the PKL ships it under, computed once during the re-encode, and the PKL's `AnnotationText` is the CPL's content title.
- **A failed job in the REST API says why it failed**: `GET /jobs` and the webhook reported `Job failed` for every failure while the cause sat in the daemon log alone. The job record now carries the runner's own error text: the verify errors, the encode's message, or the message the params that would not parse were rejected with. An operation that reports only an exit code names itself and its code.
- **A failed `create` job says why**: `create_dcp_with_progress` logged the cause and returned -1, so the daemon, `GET /jobs` and the webhook could only report `creating the DCP failed with code -1` while the cause sat in the daemon log. It returns the message now, the job record carries it, and the CLI and GUI print the same text they did before.
- **`trailer --title` reaches the CPL**: the title was drawn on the ratings card and then dropped, so the packaged trailer's `ContentTitleText` was `trailer_packaged`, the stem of the intermediate mp4, and dcpdoctor raised `isdcf_naming_violation`. The `--title` value is the content title now, falling back to the `--content` file's stem when no title was given.
- **A KDM email that fails names the cinema it was for**: `kdm-batch --smtp-config` logged `smtp send to <host> failed: ...` for a refused delivery, so a batch across a dozen cinemas said which server refused but never whose keys had not gone out. The failure now carries the label the success line already used, the cinema name or `additional recipients`. `kdm_email.rs` runs the command against a local SMTP sink: two cinemas in the cinema db each get one message at their stored contact address, with the `{title}`/`{cinema}` subject filled in and a zip attachment holding only that cinema's KDM, and with the sink answering 550 for one address the run exits 1 naming Rex while Odeon's message still arrives.

### Added
- **`dashboard matrix` is driven through the command**: the territory grid was tested inside postkit and nothing ran the CLI over it. `dashboard_matrix.rs` builds two 2K packages, registers each under its own composition id, title and territory with `dashboard register`, then exports the CSV and asserts it byte for byte: a `Territory` header with both titles in sorted order, and a row per territory with the tick in the one column that territory has a package for.
- **A PAC subtitle file is packaged and read back**: the landing page and the README name PAC, and the only test of it was postkit's parser over a fixture built inside its own test module, so nothing proved `create --subtitle cues.pac` reached a timed-text track. `pac_subtitles.rs` writes a two paragraph Latin PAC file byte by byte, builds a four second 2K DCP from it, and reads the cues back with `subtitle_extract::extract_cues` over the finished package, which resolves the track through the CPL and the timed-text MXF. Both cue texts come back, and both timings land within a frame of the milliseconds the PAC's 25 fps SSFF pairs name. postkit is untouched.
- **Every `export` format the README names is written and read back**: `export_dcp.rs` proved ProRes and H.264, so H.265, DNxHR and the image sequence were claimed with nothing running them. Each now exports the same flat colour DCP and reads the file back: H.265 as `hevc` 4:2:0 8-bit with AAC, DNxHR as `dnxhd` 4:2:2 8-bit with `pcm_s24le`, both tagged bt709/tv, and the image sequence as twelve `rgb48be` 1920x1080 PNGs, one per packaged frame. All three land within 3 percent per channel of the DCP picture they came from, and the two video formats within 6 percent of the master behind it.
- **`create` writes the job log the docs describe**: `<output>/dcpwizard.log` was the GUI's alone, so a CLI build recorded nothing about the accelerator anywhere but stderr, which a scripted run discards, while the README and the site both say the job log tells you whether the device started and how many frames it encoded. `create` now opens the log once the pre-build check has passed, so `--check` still writes nothing under `--output`, and a job that cannot create the folder or the log fails naming the path. The header carries the title, the input, the output and an `Accelerator:` line reading `off`, `requested, active` or `requested, inactive: <error>`, the same three the GUI's header uses, and the encode is followed by `[ENCODE] Frames on the device: <n> of <frames encoded>` and an `[ENCODE] WARNING:` line when the device was asked for and took no frame. `job_log.rs` reads the log back after a plain create and after one with the GPU preference set and no plugin to load.
- **HDR10+ is a grade the source detector names**: `detect_hdr_source_format` read a master's Dolby Vision RPU and its colour tags, so an HDR10+ master with no PQ tags on its base layer was refused as naming no grade, and one with them was read as plain HDR10. It now reads ffprobe's `SMPTE2094-40` frame side data between the RPU check and the colour tags, `HdrSourceFormat` has an `Hdr10Plus` grade that `--hdr-source hdr10plus` names, and `plan_hdr_dcdm` transforms it as the HDR10 base layer it is, the way a Dolby Vision profile 8.1 master is transformed. The detector's tests now cover all five grades the README claims: a Dolby Vision profile 8.1 fixture from postkit, an HDR10+ fixture built by injecting ST 2094-40 metadata into an HEVC stream with `hdr10plus_tool`, and the tagged HDR10, HLG, PQ P3-D65 and refused SDR clips it already had. CI installs dovi_tool 2.3.3 and hdr10plus_tool 1.7.2 on all three runners and prints both versions with the other test tools.
- **`hdr-convert` tone maps between HDR10, HLG and SDR**: postkit's `convert_hdr` had no command behind it, so the only tone map a user could reach was the HDR to SDR pass `create --allow-generic-hdr-tonemap` runs on the way to a package. `hdr-convert --input --output --target <hdr10|hlg|sdr>` runs it on its own, and `dcpwizard_core::dolby_vision::convert_hdr` now refuses a missing input by name before ffmpeg starts, the way `dv-inject` and `hdr10-inject` do. `hdr_metadata.rs` converts a PQ source to HLG and to SDR and an HLG source to PQ, and asserts each output carries the target transfer tag and decodes to different pixels than its source.
- **Every source container the README names is built through the CLI**: `create` was proven end to end from an ffmpeg testsrc mp4 alone, so the ProRes, H.265 and DNxHR transcodes and the MXF and AVI containers were claimed with nothing running them. `source_containers.rs` writes a three frame 2048x1080 source per format with `prores_ks`, `libx265`, `dnxhd` at `dnxhr_sq`, the mxf muxer and the avi muxer, asserts ffprobe reads back the codec name `create` routes on, builds each into a DCP and reads every packaged frame back out of the picture MXF as a 2048x1080 12-bit three component picture that is not all black, with a clean verify over each package.
- **Both watermark routes are marked through the commands**: the only end-to-end mark ran `transcode_dcp` over a cleartext package from the core API, so `create --watermark` and the `--kdm`/`--recipient-key`/`--keys` route over an encrypted source had nothing running them. `watermark_cli.rs` builds a flat grey 2048x1080 package twice, with and without `create --watermark`, and asserts the unmarked build leaves every sample at the source's own code value while the marked one raises at least 500 samples by a quarter of the 12-bit range and none of them above the band the mark anchors in. A second case builds the same source with `--encrypt --key-out`, asserts its picture essence is encrypted, marks it with `watermark --keys`, and asserts the output picture is cleartext, carries the mark in the same band, passes dcpdoctor, and that the same command without key material fails.
- **The xmeml and fcpxml conforms are built through the command**: only the EDL parser reached a finished DCP in a test, so the FCP7 and FCPX routes rested on postkit's parser unit tests. `conform_timelines.rs` writes a two clip xmeml and a two clip fcpxml over the same generated media and runs `conform --input --media-dir --output` on each, then asserts the manifest names the parser that ran (`XmlFcp`, `XmlFcpx`), the reel plan names both reels, the timeline's own title is the CPL's `ContentTitleText`, the CPL has two reels, each reel's picture track file carries the six frames the timeline trimmed it to and decodes at 2048x1080 12-bit, and dcpdoctor passes.
- **The trailer command is packaged and read back**: `trailer` had no test beyond enum defaults, so the card, the leader and the DCP wrap around them were unproven. `trailer_dcp.rs` packages three frames of content behind a green band card and a one second countdown, then behind a red band card, and asserts the picture track file carries the card's 120 frames plus the leader's 24 plus the content's 3, that the preview renders frame 0 back to the band colour and the first content frame to something else, that the CPL's `ContentKind` is `trailer`, and that dcpdoctor passes.
- **The drive copy is tested through the command**: `dcpwizard copy` had no test, and the free-space refusal `copy_to_drive` makes had none anywhere. `copy_drive.rs` copies a nested package through the CLI and compares every file's bytes and SHA-1 with the source, and a second case gives the source a 4 TiB sparse file, which the check reads at its apparent size, then asserts the copy is refused naming what it needs against what is free with nothing written into the destination. That child runs under a 64 MiB `RLIMIT_FSIZE`, so a regressed check cannot fill the temp filesystem with the sparse file's zeros.

### Removed
- **`ingest` is gone**: it matched camera RAW (ARRIRAW, R3D, BRAW, Canon Cinema RAW Light, Sony X-OCN) by file name or MXF header and refused every match, and transcoded ProRes and DNxHR through ffmpeg, which `transcode` does. Camera RAW is graded and exported as a master before a DCP is built, and `create --video` already refuses a file ffmpeg cannot decode.

## [1.2.0] - 2026-09-07

### Changed
- **The GUI preview plays JPEG 2000 through grok**: a picture MXF, a DCP directory, a CPL or a directory of codestreams loads into postkit's `GrokPlayer` instead of libmpv, so the frame the step preview draws and the frame playback draws are the same pixels. A 250 Mbit/s 2K DCP plays at its own rate where libmpv's ffmpeg jpeg2000 decoder managed a few frames a second and sat black for seconds after a load: a 2048x1080 frame costs 179 ms on one grok thread and the pool of one worker per core sustains 48 frames a second on 16 cores. With the GPU setting on, the preview decodes on the device instead: the player runs its frames as one in-memory batch through grok's accelerator plugin, the plugin's threads pull codestreams from the player's queue, and the DCI X'Y'Z' to sRGB transform runs on the device too, so a 4096x1716 DCP decodes at 33 frames a second on an RTX 3060 laptop and 4K plays in real time. An encode holds the device while it runs and the player gives way to it, and the app ends the batch on exit, since a batch left open stalls the process's CUDA teardown. The decode resolution menu changes how many DWT levels grok discards, so full, half and quarter no longer reload the file on this path. MP4, ProRes and every other source still play through libmpv, and so does stereoscopic or encrypted JPEG 2000 essence, which `GrokPlayer` refuses by name. Each load prints `[preview] backend: grok|mpv for <path>` to stderr. The QC overlays and the SRT and ASS subtitle tracks work on both. Shared with imfwizard through the guikit submodule.
- **`create` probes the source's pixel format once**: the command read the pixel format and colour tags three times, in the plan's probe, in the alpha check and again inside postkit's encode. The alpha check and the encode now read the plan's probe. Eight ffprobe runs on a create are six, about a quarter of a second.
- **A container fit scales and pads on the decoder's own planes**: postkit's picture plan no longer spells `format=gbrp16le` before its geometry, so a `--gpu create` that fits a source into its container crops, scales and pads the planar YUV the decoder hands out instead of packed 16-bit RGB. That plan costs 6.3 CPU seconds over the bare decode on 1442 frames of 2048x872 against 73.0 through the conversion. Every crop and pad offset a plan emits is on the 4:2:0 chroma grid now, so a 1998x1080 source pads onto 2048x1080 at x=24 where it was 25 and a `--fill-crop` of a 2048x872 source into 2K scope cuts 6 rows off the top and 8 off the bottom where it cut 7 and 7.
- **A named container names its DCI family**: `create --container 4k-scope` with no `--fourk` is a 4K package, so `hfr::validate_fps_resolution` applies the 4K 30 fps cap before the encode as well as at packaging and the ISDCF name spells 4K. `--twok`/`--fourk` decide only when no container is given, and `Resolution::for_raster` is the one rule both wizards read a family off a raster with.
- **A named container is the coded raster**: `create --container 2k-scope` and the GUI's 2K Scope resolution now encode 2048x858 frames and declare a 2048x858 stored and active area, where they scaled the picture into the container and then padded it onto a 2048x1080 raster with 222 black rows. `--twok`/`--fourk` with no container still force the full 2048x1080 or 4096x2160 frame. DCP-o-matic writes the same frame size, `fit_ratio_within(container ratio, full frame)` in its `Film::frame_size`, so a scope DCP is 2048x858 frames there too. The only black left is the letterbox or the pillars an aspect mismatch inside the container leaves, and none at all under `--fill-crop`. `source_fitting.rs` builds a 2K scope package from a letterboxed HD clip and asserts the essence raster, both CPL areas and a clean dcpdoctor run.
- **grok is pinned at the v20.4.6 release**: every workflow builds the tag instead of a commit on master. 20.4.6 adds the display transform request the device decode of App 2E picture uses, and every workflow builds that tag now. 20.4.5 adds the in-memory batch decompress API with 8-bit sRGB output on the device, which the preview's GPU decode uses. 20.4.4 stops a one thread decode spawning a thread per frame, which the preview did 24 times a second. A `--gpu create` of a 21312 frame 2K feature with six channel sound on this release encodes at 127.5 fps and finishes the whole DCP, sound wrap and packaging included, in 190 s (112 fps end to end) at a 1.45 GB peak, where the previous full run on the same clip took 234 s at 2.21 GB. `dcpdoctor validate --check-mxf` passes the result.
- **The audio level hint measures true peak in Rust**: the hint ran ffmpeg's loudnorm over the whole WAV to read one number off it, 107.7 s on an 888 s six channel 24-bit track. It now calls `postkit::loudness::measure_true_peak_dbtp`, which streams the file and meters each channel on its own thread, 1.0 s on the same track for the same -0.20 dBTP. A WAV the pass cannot read is still skipped by the hint and reported by the encode.
- **`create` hashes the picture MXF once**: a picture wrapped during the encode is hashed by the wrap as it finishes, and `create_dcp` then read the whole file back to hash it again for the CPL and PKL, 19 s on a 16 GB feature. `PreWrappedPicture` now carries the wrap's SHA-1 and size (postkit's `MxfTrackFile::hash_base64`) and the package declares those.
- **`create` gathers its hints beside the encode**: the audio level hint runs ffmpeg's loudnorm measurement over the whole WAV, 106 s on an 888 s six channel track, and the command waited for it before the first frame was decoded. The hint pass now runs on a thread while the picture encodes and the hints print before packaging. `--check` still waits for them and prints the count as before.
- **`create` writes the picture MXF while the video encodes**: the command encoded to a J2K directory and `create_dcp` read the whole directory back into the MXF afterwards, 60 s of the 444 s a 21312 frame feature took on the GPU path. The CLI now starts the wrap before the encode and feeds it each codestream as the writer puts it on disk, through `overlapped_picture::PictureWrapInProgress` over postkit's `encode_video_pipeline_resumable_with_mxf_feed`, under the same rules the GUI applies: encryption, 3D, padding, reel splits and versions still wrap afterwards, and so do a `--resume` (the frames already on disk never pass through the wrap) and a trim without an encode window (the codestreams are relinked after the encode). The log names the picture MXF as written during the encode, and the CLI test asserts it.
- **The hint and burn rules both wizards carried are postkit's**: `hints.rs` keeps what a DCP has of its own (Interop, packaged channel counts, the upmix, container ratios, bit rate, frame rate, 4K 3D, source speed change, pull-up, markers and captions) and calls `postkit::hints` for the sound and subtitle rules imfwizard carried a copy of, so a threshold cannot drift between the two. The Interop caption-overlap rule is now a rule read only when the package is Interop rather than one that tests the standard itself. `subtitle::check_burn_supported` is `postkit::preflight::check_burn_supported` with the DCP flag names, `audio_map::probe_channel_count` is gone in favour of `postkit::wav_io::channel_count`, and both colour-space parsers are `postkit::colour::parse_colour_space`: `create --source-colourspace` now also takes `dcip3`, `dci-p3`, `ciexyz`, `2020`, `ap0`, `ap1`, `alexa` and `arrilogc` instead of refusing them, and `colour --source`/`--target` and `dcdm --colour-space` take the same set instead of warning and falling back to Rec.709.
- **No grok binary is run any more**: a TIFF sequence handed to `create` or a GUI job is read by postkit's own loader threads and compressed by the linked grok library, which brings a still sequence the per-frame byte cap, the P3 and Rec.2020 transforms, a subtitle burn and the overlapped MXF wrap that video already had. DPX, EXR and BMP sequences decode through ffmpeg. `transcode-dcp` decodes each source frame in memory instead of through grk_decompress and a TIFF on disk, and a resize is the only step that still writes a TIFF, for ffmpeg's scaler. The `create --source-colourspace p3|rec2020|logc` refusal for image sequences is gone. grk_compress and grk_decompress are no longer looked up, and CI no longer checks for them.
- **`encode` and the EncodeJ2k job go through postkit's pipeline**: the command and the daemon job compressed stills through a loader of their own, which read TIFF only (a DPX, EXR, PNG or BMP frame failed the run even though the input filter accepted it) and budgeted every sequence as 2048x1080 at 24 fps whatever the frames were. Both now call `postkit::pipeline::run_encode_with_options`, so a TIFF sequence is read by postkit's loader threads and every other still format decodes through ffmpeg, the ratio comes from the frames' own raster, the codestreams are held under the DCI cap, and a still that cannot be read fails the run by name. The codestreams land in `<output>/j2k` rather than in `<output>` itself, and `--fps` (default 24) is the rate the bitrate is budgeted at. The job's params are `input_dir`, `output_dir`, `bandwidth_mbps` and `fps`, and `threads` is gone. A CLI test runs `encode` on a PNG sequence and decodes a codestream back at the source raster and 12 bits, the first test the command has had.
- **`transcode-dcp` decodes on loader threads**: each source frame was decrypted and decoded on the encoder's producer thread, one at a time, while the encoder threads waited. The decode now runs on postkit's loader pool through `encode_loaded_frames`, each loader holding its own reader, decrypt context and frame buffer, ahead of the encoder threads.

- **`audio_map` parses through postkit**: the `IN:OUT[@GAIN]` walk that resolved DCP lane names is `postkit::audio_mix_matrix::parse_named_audio_map` over a lane vocabulary, the same parser imfwizard uses, so the grammar and its errors cannot drift between the two. dcpwizard keeps only the lane names and the 2, 6, 8 or 16 channel rounding. A map accepts the twelve listed lane names, the filename aliases `left`, `right`, `centre`, `sub`, `lss` and `rss` no longer double as map outputs.

### Fixed
- **The GPU preference no longer stops every command when the plugin is missing**: the CLI reads the same preferences file as the GUI, so with the GPU box ticked a `dcpwizard kdm` or `verify` run from a shell without `GRK_PLUGIN_PATH` exited 1 before doing anything. A plugin that fails to load under the preference now logs the reason and the run stays on the CPU; `--gpu` on the command line still ends the run.
- **The Preview button plays the highlighted DCP**: clicking a recent entry started a preview on its own and left the timeline empty, and the Preview button ignored an opened package. A click on a recent entry or an asset row only highlights it now, the Open button and a recent entry share one open path that loads the timeline and highlights the package, and Preview is the only thing that starts playback: it plays the highlighted row, or with nothing highlighted the first reel's picture, the opened package or the output folder. imfwizard behaves the same.
- **Listing a DCP's CPLs and reading a CPL's timeline run off the GUI's main thread**: both were synchronous tauri commands, so the window stalled for as long as the reads took.
- **Opening a DCP no longer reads its picture into memory**: the CPL list the GUI builds when a DCP is opened read every asset the ASSETMAP names whole to look for a CompositionPlaylist tag, the picture track file included, on the GUI's main thread. A 23 GB picture stalled the window for seconds and then ran the process out of memory. Only `.xml` assets are read now.
- **`create-vf` and `assemble` declare the sound they ship**: a SMPTE CPL from either command carried no `CompositionMetadataAsset`, which Bv2.1 requires and which holds `MainSoundConfiguration`, because either command can replace or combine the sound and the source CPL's block need not describe the result. Both now read the PCM MXF the output's first reel with sound references, through `cpl::main_sound_from_track_file`: the MCA label subdescriptors in the header give the soundfield group and the channel in each slot, so a 5.1 track with hearing-impaired and narration channels is declared `51/L,R,C,LFE,Ls,Rs,HI,VIN` rather than the `71/...` its eight channels would imply, and a track carrying no labels keeps the layout its channel count implies. A sound MXF whose descriptor cannot be read fails the command instead of dropping the block.
- **Closing the window during a build stops the build first**: the GUI exited with the encoder threads still compressing, which crashed in grok's teardown, and the seconds the core dump of a multi-gigabyte process took to write looked like the app hanging on close. The close now cancels the running job, records the queued ones cancelled and waits for the worker to stop before the process exits.
- **The file dialogs forgot the last folder between launches**: the GUI kept the folder of the last pick in a variable, so every launch opened the first dialog at the system default again. The folder is stored in localStorage now and the first dialog after a launch opens where the last one closed.
- **Cancelling a build and then closing the app froze it**: postkit's encoder threads left on cancel without closing the frame queue, so the producer waited forever to push into the full queue and the GUI hung on exit behind the blocked job.
- **Four places read a whole essence file into memory**, the pattern that took `dcpdoctor validate --check-mxf` to 12 GB on a 23 GB picture MXF: `prepare_packaged_channels` read the source WAV and built the widened WAV in memory (a 767 MB six channel source became about 2 GB resident before a byte was written), `wrap_j2k_hdr_files` held every codestream of the reel in memory and then read the finished MXF back for its SHA-1, and `copy-to-drive` read each file whole to hash it, wrote it, and read the copy whole again. The WAV is now streamed frame by frame through 1 MiB buffers and refuses a result over the 4 GiB RIFF limit instead of truncating the size field, the HDR wrap goes through postkit's `IncrementalJ2kWrap` with the DCI HDR metadata (one frame in memory, hash streamed at finish), and the drive copy hashes the source as it streams to the device and hashes the copy with `postkit::hash::hash_file`.
- **`verify` printed `PCM file EditRate is not a supported value: 0/0` on a clean DCP**: dcpdoctor-core opened every MXF with the PCM reader, so asdcplib logged two error lines to stderr for the picture MXF, twice, while the verdict stayed PASS. dcpdoctor 34b7c4f -> 0e38987 (v0.5.1) opens only PCM essence that way.
- **The encode aimed at a compression ratio and missed the bandwidth**: every site with a bandwidth divided the source raster's bytes by it and handed grok that ratio, but postkit pads the picture to its container before grok sees it, so grok budgeted a bigger frame than the ratio was computed from and overshot, 24 percent on a 2048x872 source in a 2048x1080 container. Nothing bound the codestreams either, and a feature failed at frame 408 with a 1,312,856 byte codestream over the 1,302,083 byte cap. `create`, the GUI job, `encode`, the EncodeJ2k job, a library reel item and `transcode-dcp` now pass the bandwidth's bytes a frame, `encode::video_codestream_byte_cap(fps, mbps, stereoscopic)`, as postkit's `target_codestream_bytes`, and postkit derives grok's ratio from the frame it actually hands grok and gives grok the same number as `max_cs_size`. A job with no bandwidth still encodes at `encode::DEFAULT_COMPRESSION_RATIO`, and `encode::bandwidth_to_ratio` and `encode::video_compression_ratio` are gone. `create` now hands the encode the DCI cap (the HDR Addendum's under `--hdr-dci`) whether or not a PSNR target is set, as the GUI job always did, so a `--video-bit-rate 300` job writes 1,300,096 byte codestreams where it wrote 1,559,896 byte ones over the 1,302,083 byte cap, and both log the target as `Target: 1197916 bytes a frame (230 Mbit/s), cap 1302083 bytes`. A 1442 frame 2048x872 source at 230 Mbit/s and 24 fps writes a largest codestream of 1,197,915 bytes against the 1,197,916 the bandwidth allows, none over it, 1195 frames within 3 percent of it, and dcpdoctor passes the package.
- **The QC report could not measure the packaged sound**: `report::collect_sound_levels` handed postkit a `sound_<uuid>.mxf` and got "wav i/o: Ill-formed WAVE file: no RIFF tag found" in the Leq(m) cell instead of a level, because the loudness passes opened their input through hound. postkit reads a PCM MXF as well as a WAV now, in process with no ffmpeg, so the report measures the track file the package ships and the `loudness` command takes either. The numbers moved with it: Leq(m) weights each channel on its own, sums the corrected channel powers, and is calibrated to the ISO 21727 table and leqm-nrt, so a 15 minute 5.1 track reads 80.36 dB where the ffmpeg mono downmix read 84.00. Every Leq(m) reported before this reads 2.6 to 3.0 dB high.
- **The GUI job log exists and says what encoded the picture**: `run_job` opened `dcpwizard.log` in the output folder before anything created that folder, and threw the error away, so no job ever wrote a log and every line went to stderr, which a desktop launch discards. The folder is created first now, and a folder or a log the job cannot create fails the job with the path in the message. The header gained an `Accelerator:` line reading `off`, `requested, active` or `requested, inactive: <error>` from guikit's `accelerator_status`, and the picture encode logs `[ENCODE] Frames on the device: <n> of <frames encoded>` from `postkit::grok_encoder::accelerated_frames`, followed by an `[ENCODE] WARNING:` line when the GPU was requested and the device took no frame. A still hold reports no count.
- **A burn file that is also the caption file was accepted**: the pre-encode burn check compared `--burn-subtitle` against `--subtitle` only, so passing the same file to `--burn-subtitle` and `--ccap` drew the cues into the picture and packaged them as a caption track as well, the double subtitle a `--subtitle` clash is refused for. The check now covers both timed-text tracks and refuses the caption clash with the same message.
- **A 4K DCP could not be built**: every encode asked grok for the Cinema 2K profile whatever the raster, and grok refuses a frame past 2048x1080 under it, so `--fourk` died at "Failed to initialize Grok compressor" on the first frame and no 4K package was ever produced. postkit now writes a frame over 2048x1080 under the Cinema 4K profile with the six decomposition levels DCI 4K carries, and `picture_readback.rs` builds a 4096x2160 package through the real create path, asserts the codestream declares Cinema 4K, and runs dcpdoctor over it, the first 4K package any test here has made.

### Added
- **`--gpu` and the GPU setting**: `--gpu` on any command runs grok's accelerator plugin for the whole run through `postkit::grok_encoder::use_gpu`, refuses to start when no plugin loads, and logs how many frames the device took at the end. The settings checkbox in the GUI does the same through guikit's `set_gpu` command, off by default. grok finds the plugin through `GRK_PLUGIN_PATH`, the working directory or the executable's directory, and `GRK_NO_PLUGIN` keeps everything on the CPU.
- **The video build is read back too**: `picture_readback.rs` gained a case that encodes a solid red clip through `postkit::pipeline::run_encode_with_options`, the path `create --video` takes, packages it and runs the same assertions the held-frame case runs: AS-DCP JPEG 2000 essence, a DCI cinema Rsize with three unsubsampled 12-bit components at the coded raster, and a decoded centre pixel within 40 codes of `DcdmTransform::to_xyz(Rec709).pixel([65535, 0, 0], 4095)`. The still path reaches X'Y'Z' through postkit's `colour::rgb_to_xyz_inplace` and the video path through grok's `apply_xyz_transform`, so until now nothing proved the two agree on a produced package
- **A produced DCP's picture is read back and checked for what it is**: `picture_readback.rs` builds a flat pure-red Rec.709 DCP through the real encode and wrap path, then reads the picture MXF on its own and asserts the essence type is AS-DCP JPEG 2000 rather than AS-02, the descriptor's codestream header carries a DCI cinema Rsize with three unsubsampled 12-bit components at the coded raster, and the decoded centre pixel of frame 0 is X'Y'Z' within 40 codes of `DcdmTransform::to_xyz(Rec709).pixel([65535, 0, 0], 4095)`. All three components of X'Y'Z' red are large, where an RGB encode of the same red would give about (4095, 0, 0), which is what the assertion messages say so a future failure explains itself. A second case runs `postkit::preview::extract_frame` on the same MXF and checks the centre of the `.ppm` it writes comes back red, so the DCDM inverse the wizard's preview shows is covered too. Nothing in the picture path was proving any of this: the samples were X'Y'Z' because the defaults said so, which is how imfwizard shipped DCP essence inside IMF packages for two months
- **`frame-extract` reads encrypted picture essence**: `--key <32 hex chars>` or `--keys-json <KEYS.json>`, where an encrypted picture MXF was refused outright before because extraction had nowhere to take a key. The KEYS.json path matches the picture track file's own asset UUID rather than taking the first image key in the file, so it picks the right key for a package carrying a picture asset per reel. A key given for an input that is not JPEG 2000 MXF essence is refused rather than ignored, and one given for unencrypted essence warns and goes unused. The two flags are mutually exclusive. `--key` leaves the key in the process list where other users of the machine can read it, so its help says to prefer `--keys-json`
- **`report --scan-picture` finds black and frozen runs in a finished package**: every encode has reported them for a while, but a report on a package this machine did not build carried no such pass. The flag decodes each picture track and lists what it finds per reel in a Picture section, in the same wording the encode logs: `black picture from 00:00:00:00 to 00:00:02:22 (frames 0 to 70)`, numbered from the track file's own first frame. Encrypted picture is listed and skipped with the reason, since ffmpeg cannot decrypt AS-DCP, and a track it cannot decode names the error rather than passing as clean. The findings are advisory: a black head or tail and a held still are legitimate, so a track with runs reads `review` and one without reads `clean`. The scan is off by default because the decode is ffmpeg's software J2K one, a few frames a second at DCP bitrates, and a report made without it says the picture was not scanned rather than leaving the section out
- **`watermark` styles its mark and names its encoder**: `--font-size`, `--colour`, `--position top|center|bottom`, `--video-codec` and `--crf`, where the mark was fixed at 24-pixel white text along the bottom and the copy took whatever encoder ffmpeg guessed from the output name. The colour takes any ffmpeg colour name, which is what the drawtext branch reads, and the defaults are what every mark carried before, so an existing command line produces the same file
- **The standalone `burnin` names its encoder and styles its cues**: `burnin` gained `--colour <RRGGBB>`, `--position top|center|bottom`, `--video-codec <encoder>` and `--crf <n>`, where before it took only a font size and left the encoder and its quality to ffmpeg's guess from the output file name. `--video-codec libx264 --crf 0` writes a lossless copy, so a review pass no longer costs an unnamed lossy transcode. The command is for a video file rather than a package, and its help now says so: a DCP burn goes through `create --burn-subtitle`, which draws the cues during the encode and costs one generation
- **Black and frozen picture is reported while the encode runs**: postkit runs ffmpeg's `blackdetect` and `freezedetect` on a branch of the decode, and each run it finds comes out as `black picture from 00:00:00:00 to 00:00:02:23 (frames 0 to 71)`, timecoded at the encode's rate. `create` and `pipeline` print them as warnings, the GUI writes them into the job log as `[ENCODE]` lines, a right eye's are prefixed `right eye:`, and a joined head or tail library item's are prefixed `library item '<NAME>':`. A trim numbers them from the first frame it kept, the way it numbers the codestreams it writes. A run has to last 2 seconds to be reported, ffmpeg's own default for both filters. A held still, an image sequence and a J2K directory report nothing, since none of them decodes through ffmpeg
- **A quality target for the J2K encode**: `create --quality-psnr <dB>`, and the GUI create panel's Quality (PSNR dB) field, hand grok a PSNR target to allocate layers by instead of a compression ratio. The target is at least 20 and at most 80 dB, refused outside that before anything is encoded. The bandwidth (`--video-bit-rate`, the `--profile` preset's, or the GUI's Bandwidth field) stops being what the allocation aims at and becomes a per-frame byte cap: 250 Mbit/s at 24 fps is 1,302,083 bytes, halved per eye for a 3D encode the way the ratio is doubled, and never above the DCI cap the build already held to (the HDR Addendum's under `--hdr-dci`). A frame the quality target pushes over the cap is compressed again by ratio, so the cap is a ceiling no codestream passes. Leaving the field blank, or the flag off, keeps the ratio allocation unchanged
- **The Jobs panel lists the GUI's own builds**: the panel used to print the daemon's `batch list` and nothing else, so a GUI build was invisible from the moment it was queued. It now lists both queues in one table with a Source column reading `gui` or `daemon`, GUI rows coming from the `list_jobs` command: the running build, then the queued ones, then the finished ones newest first, each with the state word the daemon uses (`running`, `queued`, `done`, `failed`, `cancelled`) and the failure message on the row. The finished ones include the jobs the last run left behind, read out of `gui-jobs.jsonl` at launch, which is where a build that was running when the window closed comes back as failed. Cancel on a running or queued GUI row calls the same `cancel_job` the progress bar's Cancel calls. With the daemon down the badge still reads Offline and the GUI rows are still listed, and "No jobs" now means both queues are empty
- **A queued job survives a crash, a reboot or a closed window**: the daemon writes one JSON line per job to `~/.local/share/dcpwizard/jobs.jsonl` when a job is submitted and on every state change after it, and reads the file back before the queue processor starts, so the queue no longer lives only in memory. The last line for a job id is its state: a Pending job is queued again and runs in `created_at` order, a job left Running is loaded as Failed with "the daemon stopped while this job was running" since a half-run job cannot be picked back up, and a finished job stays in the file so `batch list` still lists it. The file is rewritten with one line per job on load, and a line that does not parse is logged with its line number and skipped rather than stopping the load. `DCPWIZARD_JOBS_FILE` points a second daemon, or a test, at a file of its own. The GUI's Jobs panel keeps its own queue the same way in `gui-jobs.jsonl`: a job queued when the window closed is back in the panel at launch and runs when the queue next runs, a job that was building comes back failed, and nothing starts an encode on its own at launch
- **Transport buttons in the preview**: to-start, skip back and forward by 5 seconds, and single-frame step back and forward either side of play, with `,` and `.` as the frame-step shortcuts. Frame stepping pauses playback, as mpv's `frame-step` does. Shared with imfwizard through the guikit submodule.
- **The picture MXF is written while the encode runs**: a GUI build hands each finished codestream straight to the wrapper instead of encoding the whole picture, then reading the J2K directory back to wrap it, so packaging no longer pays for a second pass over the essence. The MXF is the same file either way, byte for byte, which is what postkit's overlapped wrap is tested against. One predicate decides it before the encode starts, and the job log names the reason when the answer is no: a held still, an image or J2K sequence, a head or tail trim, 3D, head/tail padding, a split composition, `--versions` and `--encrypt` all take the encode-then-wrap path unchanged, the last because the picture content key is minted while the package is written. A cancel or a failure on either side deletes the part-written MXF and leaves the codestreams that finished, so a resumed build still has them. `create` on the command line is unchanged: it encodes through the resumable encoder, which writes codestreams and has no frame feed for a wrap to read
- **A playlist that plays packages one after another**: the preview area gained a queue under the scrubber, and a package reaches it from the `+` on a Recent Projects row or the Queue button beside a finished build. A row shows its position and title, moves up or down, comes out, and plays the queue from wherever it is clicked, the row playing carrying a marker and taking the composition title mpv reports once the package is loaded. When a package reaches its last frame the next row loads and starts on its own, off the `eof` the metadata poll now carries rather than a timer of its own. A row plays through the same `previewPackage` the Play button uses, so a queued package arrives with its packaged subtitle and caption tracks attached and the crop overlay cleared, rather than with the crop of whatever was previewed before it still drawn over the frame. Previewing anything else, from the toolbar or a reel's picture, leaves the rows where they are and stops any end of file advancing them until a row is clicked again. The queue lasts the session: nothing is written down and there is no playlist file. Shared with imfwizard through the guikit submodule.
- **The Recent Projects panel starts out of the way**: the panel is collapsed on first run and the header toggles it, with the last state kept in localStorage, so a long list of packages no longer pushes the Assets panel down the moment the app opens. The rows, the retitle and the delete are unchanged.
- **TMS upload**: `tms <package-dir>` puts every file of a finished package into `<path>/<package dir name>/` on a theatre management system over sftp or ftp, and `create --upload-to-tms` does it straight after a successful build, before `--shutdown-when-done` powers the machine off. The protocol, host, optional port, remote path, user and password come from a TOML file, `~/.config/dcpwizard/tms.toml` unless `--tms-config` points elsewhere, read the way `--smtp-config` is: the password is redacted in Debug, never logged, never echoed in an error and never passed as a command-line argument. Remote directories are created before the files that go in them, each file logs an `[n/total] Uploaded` line, and the first failure stops the upload naming the file and the remote path it was going to rather than leaving a half package with a success. An unreadable config fails before the encode starts instead of an hour later. An sftp host is checked against `~/.ssh/known_hosts` and refused when its key is unknown or has changed, with the fingerprint and the `ssh-keyscan` line that would record it, since libssh2 checks no host key by itself and an unchecked upload hands the TMS password to whatever answers on the address. Plain ftp sends the login and the package unencrypted and says so, so sftp is the protocol to use wherever the TMS offers it
- **A library of head idents, tail idents, rating cards and anti-piracy clips joined onto the build**: `library add --file <clip> --name <NAME> --kind head-ident|tail-ident|rating-card|anti-piracy` copies media into a managed folder under the XDG data dir with an index recording what a build reads off it (raster, running time, whether it carries sound), `library list` prints what is there and `library remove --name` drops an item and the copy that came with it. `create --head-item NAME` and `--tail-item NAME`, both repeatable and joined in the order given, turn those items into reels of their own: head items are the CPL's first reels, tail items its last, and the feature's essence and reels are untouched. An item is conformed to the job at build time rather than at import, so one 30 fps 1920x1080 ident serves a 24 fps 2K build and a 25 fps 4K one. It is fitted onto the feature's coded raster with the container as the active area, decoded through the job's own `fps` filter, encoded at the feature's bit rate through the same DCI transform, and its sound is panned into the same lanes at the same sample rate and cut to exactly the reel's length, filled with silence for an item that carries none and for one whose sound runs short of its picture. A still is held for the duration given at import and costs one encode, the trick `--still-length` already uses. Subtitle and caption tracks stay on the feature's reels and never extend into an item's. FFOC and LFOC now describe the composition rather than the feature, so a head ident carries the first-frame marker, and `--marker` is refused with items joined on as it already is for a split composition. So are 3D, Atmos, `--hdr-dci` and `--versions`, none of which an item reel has a place for. Reel splitting is fine: the feature's reels split as before and the items sit either side. In the GUI it is an Ident Library panel with an import button, a kind select and two lists, Head and Tail, that items are dragged into and reordered in
- **A container choice fits the picture** (GUI): picking 2K Flat/Scope/Full or a 4K container scales the source onto that DCI raster with the container as the active area, letterboxed unless Fill container is ticked. Before, an untouched Fill box left the source at its own size and the build was refused for declaring an active area the frames did not have.
- **Where the time inside an encode went**: the job log's `[TIMING] encode took` line is followed by `[TIMING] encode breakdown: decoder wait 12s, frame prep 30s, j2k 4m10s, write 8s`, read off the four cumulative phase clocks postkit's `PipelineProgress` now carries. Frame prep and j2k are summed over the encoder threads, so either can come out longer than the encode's own clock. `create` prints the same four figures after the frame count it already logs. A held still and an image sequence handed straight to grk_compress time nothing, and the line is left out there rather than printed as four zeros
- **Sound level in the QC report**: `report` gains a Sound level section listing every sound track file of the package with its Leq(m) (ISO 21727, CCIR 468 weighted, measured through postkit's `loudness::measure_leq_m`), the limit for the composition's `<ContentKind>` and a pass or fail against it. Advertisements are held to 82 dB and trailers to 85 dB, both stated inline with where the numbers come from; a feature is shown against the 85 dB reference level and marked not applicable, as is any other content kind. An encrypted sound track is listed and skipped with the reason instead of being measured through a key the report does not have
- **`--encrypt` encrypts every essence**: the subtitle, closed-caption and Atmos tracks are now AES-128 encrypted at wrap time alongside picture and sound, each with its own content key. The CPL declares a `<KeyId>` on every asset block including `AuxData`, the keys file carries all of them, and the KDM `kdm` builds from it lists one key per essence with its ST 430-1 type: MDIK for picture, MDAK for sound, MDSK for timed text and MDEK for aux data, the same spellings libdcp and DCP-o-matic write. `--encrypt` used to refuse a package carrying any of those three tracks rather than ship it in the clear beside encrypted picture and sound, so an encrypted subtitled package could not be built at all and one built without the track failed a validator's partial-encryption rule
- **The crop and the subtitle tracks in the preview**: opening the preview on a reel's picture draws the crop the Source picture panel holds over the frame and loads that reel's subtitle file and the closed-caption file as the two subtitle tracks, so the Crop, Sub and CC buttons in the preview header have something to show. The crop follows the four fields as they are typed and the Auto-crop button as it fills them. mpv renders only SRT, ASS/SSA and WebVTT, so anything else is written out as SRT under the app cache folder first, DCST and Interop XML through `subtitle_extract` and PAC, MKS/MKV, FCPXML and Interop PNG through the same `load_styled_cues` the create path reads cues with. Previewing a built DCP reads both tracks back out of the package instead, cues placed on the composition timeline, and draws no crop because the pictures already carry it. A file the preview cannot read leaves its button inert with the reason in the status bar rather than stopping playback
- **Picture essence read for the QC report**: `report` runs the verify with `check_picture_details` and `scan_every_frame` on, where `verify` leaves the frame scan off and stays fast, so the report's Info rows carry dcpdoctor's codestream forensics line beside the measured picture bitrate
- **Packaged channel count**: `create --audio-channels 2|6|8|16` fills the sound track with silent channels up to the count asked for, so a stereo source ships in a 16-channel container without being upmixed to 5.1 first. Without the flag nothing changes: a 5.1 source is widened to 16 and every other source is packaged at its own width. A source carrying more channels than the count is refused before the encode and again at wrap time, naming both counts and pointing at `--audio-map` to fold channels. The labels follow the content rather than the container, so a stereo track filled to 16 carries `20/L,R,-,...` in the CPL's MainSoundConfiguration and stereo MCA labels in the sound MXF, where the fill channels used to be claimed as a 5.1 mix they carry nothing of. The "fewer than 6 channels" hint now names the flag that fixes it, and the GUI create panel has a Packaged channels select beside the upmix one
- **Preview QC controls** (guikit): the embedded preview's header gained a safe-area box (95% or 90%), an aspect mask (1.85, 1.90, 2.39), a centre cross, a thirds grid, a decode-scale select (Full, Half, Quarter, native to J2K through the decoder's `lowres`) and a HUD reading frame, fps, buffer depth and dropped frames. Shared with imfwizard through the guikit submodule.
- **Post-build Play / Inspect / Reveal**: a finished build shows the three buttons beside the progress bar, so the new DCP can be acted on from where the build was watched. Play opens it in the embedded preview through guikit's `previewDcp`. Inspect switches to the Verify view, points it at the output folder and runs the same verification the Validate button runs, calling that code rather than a second copy of it. Reveal opens the output folder in the file manager. Starting another build clears the row, so a cancelled or failed build never leaves buttons pointing at a package that was not written
- **Per-stage timings in the job log**: `run_job` clocks preflight, encode, audio, packaging and validation, and writes each as `[TIMING] <stage> took <m>m<s>s` next to that stage's own `[ENCODE]`, `[PACKAGE]` and `[VALIDATE]` lines, with a `[TIMING] total` at the end. The progress event is unchanged: its `elapsed_secs` already carries the encode's own clock from postkit, and the breakdown inside an encode (colour convert against frame prep against the J2K encode) would need a wider payload from postkit, recorded in DESIGN_TODO
- **The encode runs at the rate it is given**: postkit's encode seams now take a rational `FrameRate` rather than whole frames per second, so a fractional rate reaches ffmpeg's `fps` filter as `24000/1001` instead of `24`. DCP edit rates are whole numbers (SMPTE 428-11 / 429-13 define no 1001-based rate), so `create --frame-rate` and the GUI's Frame Rate menu are unchanged and every seam here passes `FrameRate::whole`. A 23.976 source packaged at 24 fps is still the deliberate speed-up it always was, with the 1000/1001 audio pull-up that goes with it
- **A pre-build check, so a job that cannot be packaged fails in a second** — every refusal `create` can make now fires before the encode, from one shared check over one description of the job. Four of them used to wait for the wrap, an hour of J2K later: a container larger than the frames the encoder produces, a sound sample rate that is not a whole number of samples per frame, an Atmos track whose file count does not match the picture, and a `--subtitle` or `--ccap` file the packager cannot read. So did the refusals a split composition makes (3D, Atmos, `--hdr-dci`, markers), the encrypted-package track check, and the `--audio-map` parse. `create --check` runs the whole check plus the hints and stops, encoding nothing and writing nothing under `--output`
- **Hints before you build** — advisory findings gathered before the encode, from DCP-o-matic's projector-facing list: an Interop package, a sound track with fewer than 6 channels or a count that is neither 8 nor 16, the experimental stereo upmix, Scope content letter-boxed in a Flat container (and the pillar-boxed and unusual-container cases), a video bit rate at or above 245 Mbps, 25/30/48/50/60 fps, 4K 3D, a source-to-DCP speed change that shifts the sound's pitch, audio peaking above -3 dBTP, sound with no language, a SMPTE feature with no FFEC and FFMC markers, and the subtitle and caption rules (first cue before 4 seconds, cues under 15 frames, gaps under 2 frames, more than 3 lines, lines over 52 or 79 characters, caption lines over 32, overlapping Interop captions). Nothing here refuses a build. The CLI prints them before the encode, and the GUI shows a "Before you build" dialog with Build anyway and Go back, silenced from the dialog or the Settings pref "Show hints before building" and still written to the job log
- **Burn-in during the encode** — `create --burn-subtitle <file>` renders the cues into the picture as it encodes, one decode and one encode, instead of the transcode-then-encode the standalone `burnin` command costs. It takes the same inputs `--subtitle` does (SRT, ASS/SSA, PAC, MKS/MKV, FCPXML, Interop DCSubtitle with PNGs), and works on every input shape that encodes: a video, an image sequence (which reaches the frame path through ffmpeg's concat demuxer) and a held still, which encodes once per cue change instead of once per frame. `--burn-subtitle-font <ttf/otf>` picks the face; without one the system fonts are used, and a machine with no font is an error rather than a blank picture. Burnt-in text is part of the image, so it registers no timed-text track, and passing the same file to both `--burn-subtitle` and `--subtitle` is refused. So is burning onto frames that are already X'Y'Z' (`--source-colourspace xyz`, `--hdr-already-pq`, `--hdr-to-dci-lut`), where display-RGB text would land in the wrong colour space, and onto a J2K directory, where nothing decodes. Also on the GUI create panel
- **Subtitle appearance**: `create` now decides how subtitles look, burnt in or packaged. For a burn: `--burn-font-size` (percent of the frame height), `--burn-colour`, `--burn-effect none|outline|shadow`, `--burn-effect-colour`, `--burn-outline-width` (percent of the text height), `--burn-line-height` (multiple of the text height), `--burn-margin` (percent of the frame height), `--burn-x-scale`, `--burn-y-scale`, `--burn-fade-up` and `--burn-fade-down`. For the packaged timed-text track: `--subtitle-font-size` (points), `--subtitle-colour`, `--subtitle-effect`, `--subtitle-effect-colour`, `--subtitle-fade-up` and `--subtitle-fade-down`, which write the ST 428-7 `Font` attributes and the per-cue fades, an outline spelled `border` as the standard spells it and a fade rounded to whole frames at the edit rate. Anything left unnamed keeps the value it always had, so an existing command line produces the same package. An appearance flag given without the track it styles is refused by name, `--ccap` keeps the default appearance, and colours are written `RRGGBB` or `RRGGBBAA`. Both sets are on the GUI create panel, apart from the two burn-in scales
- **Window state restore** — the GUI reopens with the size, position and maximized state it was closed with (tauri-plugin-window-state)
- **Composition identity in the CPL** — `create` now writes the `AnnotationText` Bv2.1 requires (equal to the content title, on both CPL and PKL), a `VersionNumber` in the CompositionMetadataAsset, and a `<Hash>` on every reel asset, which some servers check instead of the PKL's. Our own validator flagged all four on our own output
- **ISDCF content titles**: `create --isdcf-name` rebuilds the title to the ISDCF naming convention from what the package actually carries, so `--title "My Film"` on a stereo flat feature comes out `MyFilm_FTR-1_F_EN-XX_20_2K_ABC_20260816_SMPTE_OV`. `--audio-lang`, `--rating AGENCY=LABEL`, `--content-version`, `--studio`, `--territory-type`, `--isdcf-date`, `--temp-version`, `--pre-release`, `--red-band`, `--two-d-version-of-three-d` and `--version-file` feed the name, and the metadata among them lands in the CPL whether or not a name is built from it. The channel ladder is read from the packaged sound, so the name is built after the audio is prepared, and burnt-in subtitles are spelled in lower case where an open track is upper. `--output` is untouched. In the GUI it is a Settings toggle with a live preview under the title, plus a Naming and metadata fieldset
- **Ratings and content version in the CPL**: `create --rating` fills the ST 429-7 `RatingList` and the first `create --content-version` becomes the `ContentVersion` LabelText, both of which every created CPL previously left empty
- **ST 429-16 composition metadata** — `create --release-territory/--version-number/--chain/--distributor/--facility/--luminance`. A numeric territory declares the UN M.49 scope, without which readers measure it against the RFC 5646 region grammar and reject it
- `create-multi --container-dims`, matching `create`
- **KDM formulation and forensic marking** — `kdm`, `kdm-batch` and `kdm-rewrap` take `--formulation` (the four ISDCF Doc 5 spellings), `-p/--disable-forensic-marking-picture` and `-a/--disable-forensic-marking-audio [CHANNEL]`, which press and festival screenings are routinely ordered with. Omitting `--formulation` still derives it from whether `--device-cert` was given, so nothing changes for a caller who does not care. `kdm-batch` refuses a device-listing formulation up front, since a batch spans cinemas and carries no device list to name
- **Audio delay** — `create --audio-delay <ms>` shifts the sound against the picture, positive later and negative earlier. The running time is unchanged, so the shift is made up with silence at the other end and the sound still matches the picture duration the CPL declares; a delay at least as long as the programme is refused, naming both durations
- **Trim** — `create --trim-start`/`--trim-end` (`48f`/`2s`) cut the head and tail off the source. Picture, sound and subtitles move together: cues slide back by the head trim, a cue wholly outside the kept range is dropped and one straddling a boundary is clamped. Trim runs before padding, so `--trim-start 2s --pad-head 1s` drops two seconds and then prepends one. A trim that leaves nothing is refused
- **Still images** — `create --video card.png --still-length 5s` packages a single image held for a duration. The image is encoded once and its codestream linked for every frame, so a two-minute card costs one encode. A still without a length, or a length without a still, is refused
- **Source colour space** — `create --source-colourspace <rec709|p3|rec2020|xyz|logc>` declares what the source carries. `rec709` (the default, byte-identical to before) leaves the compressor doing the DCI X'Y'Z' transform, `xyz` compresses untransformed, and `p3`, `rec2020` and `logc` are converted frame by frame during the encode with the compressor's own transform off. `logc` decodes with ARRI's LogC3 curve for EI 800 and matrixes through ALEXA Wide Gamut, so a LogC3 mid grey card (code 0.391) lands on the same X'Y'Z' codes as 0.18 scene linear through Rec.709, and scene linear above 1.0 clips to white rather than being tone mapped. ACES and ACEScg are refused rather than approximated: they are scene-referred, so they need a rendering transform, either a 3D LUT through `--hdr-to-dci-lut` or a conversion to X'Y'Z' before the build. A colour space other than the default is also refused for J2K input, where the picture is already encoded and no transform can run, and for an image sequence grk_compress reads straight from file, which only converts Rec.709
- **Gain and fades** — `create --audio-gain` in dB, `--audio-fade-in/--audio-fade-out` and `--video-fade-in/--video-fade-out` in seconds. All four keep the running time the same, so the sound still matches the picture duration the CPL declares; a fade longer than the programme is refused
- **CPL markers** — every created composition now carries a ST 429-7 `MainMarkers` asset in reel 1 (Bv2.1 expects one), with FFOC/LFOC by default. `create --marker LABEL=timecode` (repeatable) places any of the ten defined markers, e.g. the FFEC/FFMC distributors ask for; the offsets are validated against the composition length. Previously the `markers` subcommand printed a MarkerList nothing read

### Changed
- **The GUI build takes libmpv from an action on every OS**: `PostPerfection/setup-libmpv@v1` replaces the `libmpv-dev` apt line in ci.yml's gui job and gui-release.yml, and gives the macOS and Windows release builds the libmpv they never had. postkit's build.rs wants `MPV_LIB_DIR` on Windows, which the action sets from a shinchiro mpv-dev archive after generating an MSVC import library from the DLL, so the Windows GUI release compiles instead of failing at link time
- **CI runs every test instead of skipping about thirty of them**: around 25 ffmpeg presence guards, the grk_decompress lookups in `source_colourspace.rs`, `pad_color.rs` and `subtitle_burn.rs`, the `DCPWIZARD_ASDCP_INFO` guard in `asset_uuid.rs`, the xmlsec1 and xmllint guards in `package_signature.rs`, `conform_assembly.rs`, `subtitle_track.rs`, `subtitle_formats.rs`, `kdm.rs` and `package_signature.rs`, and the system-font and J2K-encoder guards all printed a line and returned, so a run of 0 failures said nothing about whether the code had executed. Each is now a hard failure. The workflow installs what they need: a pinned ffmpeg 8.1 static build on Linux and Windows in place of apt's 6.1 and chocolatey's, which gives the APV decoder `apv.rs` needs, `libxml2-utils` and `xmlsec1` and `fonts-dejavu-core` on Linux, `xmlsec1` on macOS, `libxml2[tools]` on Windows for xmllint, and a `Check test tools` step that runs each binary before the build. `asset_uuid.rs` resolves asdcp-info out of the asdcplib-sys build script's OUT_DIR, with `DCPWIZARD_ASDCP_INFO` still overriding it. `disk.rs`'s two tests are `#[cfg(target_os = "linux")]` and fail when /proc/mounts names no /dev source or mkfs.ext2 is not on PATH, which `e2fsprogs` installs. The runners carry two major versions of xmlsec1, 1.2.42 on Windows against 1.3.12 on macOS, and 1.3 searches keys strictly and prints no error detail, so every xmlsec1 call probes `--help-all` once for `--lax-key-search` and `--verbose` and passes what that binary accepts. macOS takes ffmpeg from conda-forge rather than Homebrew, whose build carries no libass or freetype and answers the subtitle burn tests with "No such filter: subtitles". Every assertion on an external tool's exit status now prints that tool's stdout and stderr. `tests/test_cli.py` and `tests/test_gui.py` ran in no workflow at all and now run under uv, the GUI one under xvfb against a real release build of the Tauri app
- **The picture descriptor carries the codestream header it wraps**: the AS-DCP JP2K `PictureDescriptor` gained a `codestream` field parsed from the essence's first frame, replacing the `component_count` the descriptor used to be built with, so the MXF sub-descriptor describes the codestream that is really in the file. `asdcp-info -d` on a produced DCP now reads `Rsize: 3`, `Xsize: 2048`, `Ysize: 1080` and a filled CodingStyleDefault where those were zeros before. The DCI HDR wrap parses the header and fails loud naming the frame if the codestream will not parse, rather than wrapping a descriptor of zeros
- **`--subtitle-rtl auto` asks the bidi algorithm which cues are right to left**: the test was six Unicode code-point ranges covering Hebrew, Arabic and Syriac, so a cue in any other RTL script (Thaana, N'Ko, Adlam, Arabic Extended-B) went out in logical order and mirrored on a server that does not reorder. It now calls `postkit::subtitle_formats::bidi::has_rtl`, which is the same Unicode bidi pass that does the reordering, so what gets reordered and what decides to reorder agree. A cue with no RTL run in it is untouched as before, and imfwizard can use the same call
- **The GUI's job queue and Jobs panel are the shared ones**: the queue is now `postkit::gui_job_queue::GuiJobQueue<JobConfig>` and the panel guikit's `jobs.js`, so dcpwizard and imfwizard run the same code instead of two copies that can drift apart. `gui/src-tauri/src/pipeline.rs` keeps `JobConfig` and its `GuiJob` impl, `gui/src-tauri/src/job_store.rs` is gone, and `gui/src/main.js` hands `initJobsPanel` the table elements plus an `extraRows` hook running `batch list` and `batch cancel` for the daemon's rows. The panel looks and behaves as it did. `DCPWIZARD_GUI_JOBS_FILE` now points the GUI's `gui-jobs.jsonl` somewhere else, the way `DCPWIZARD_JOBS_FILE` points the daemon's `jobs.jsonl`
- **A trimmed video build wraps its picture MXF while it encodes**: the windowed encode produces exactly the frames the composition ships, so the trim no longer forces the encode-then-wrap path.
- **A trim encodes only the frames it keeps**: `--trim-start` and `--trim-end` reach the encoder as a frame window, so a job keeping five minutes of a two-hour source compresses five minutes rather than compressing the whole source and relinking the kept codestreams out of it afterwards. The frames outside the window are still decoded and thrown away, which costs a small fraction of compressing them. `trim::encode_window` is the one place that decides, so `create` and the GUI cannot answer it differently, and both eyes of a 3D job take the same window. A codestream directory is never encoded, so its kept frames are still hardlinked into a fresh directory renumbered from zero, and so are the frames of a source whose length was never probed. The sound is still cut sample-accurately after any pull-up, and the timed text still slides against the same head-frames-plus-kept-frames window. `--resume` now records the window's frame count instead of the source's, so changing a trim refuses the resume rather than reusing another window's codestreams. `--video-fade-out` is now placed at the end of the kept window, where a `--trim-end` used to cut the fade away entirely. `--video-fade-in` still starts at the source's first frame, so a `--trim-start` still drops it

### Fixed
- **A 3D GUI build with a trim shipped mismatched eyes**: the GUI trimmed the left eye and handed the packager the right eye's untrimmed frames, so the two tracks held different frame counts. Both eyes now encode the same window. `create` trimmed both and was never affected
- **`decrypt` and `transcode-dcp` wrote a CPL with no CompositionMetadataAsset**: both rebuild the CPL around the sound MXF the source package ships, so they now read that package's `MainSoundConfiguration` and `MainSoundSampleRate` and declare the same layout, where they used to declare none and leave dcpdoctor reporting `missing_required_element` on output that was otherwise clean. An Interop source carries no such block and its output still has none
- **A build with an empty Sound slot shipped with no sound at all**: the GUI's audio chain now begins by extracting the picture source's own audio track when no sound file is named, so an mp4 that carries sound packages that sound, with the map, upmix, delay, loudness and pull-up steps applied to it exactly as they are to a named WAV. `create` has demuxed the source since it was written, and only the GUI threw away the audio in the file it was handed. With no audio anywhere, a SMPTE build now packages silence as long as the picture, 5.1 wide or as wide as `--audio-channels` asks, instead of leaving the composition with no sound MXF. That also gives the CPL its CompositionMetadataAsset (ST 429-16), which carries MainSoundConfiguration and so cannot be written for a package with no sound track, the block dcpdoctor warned about on every soundless SMPTE package we built. Interop is unchanged, picture only and with no metadata asset
- **The DCI bitrate cap is enforced on SDR encodes**: the GUI's encode now passes postkit the 250 Mbps per-frame byte budget (`dci_codestream_byte_cap`) whenever `--hdr-dci` is off, where it used to pass no cap at all and only HDR jobs were checked, and the `pipeline` subcommand encodes through the same in-process encoder with the same cap, so `GRK_COMPRESS_BIN` no longer affects it. Either way the run stops on the first codestream over the cap, naming the frame, instead of encoding the rest of the clip first. An over-budget frame used to ride silently into the package and surface as `j2k_bitrate_exceeded` at QC. The overshoot itself, up to 14 bytes a frame on dense content at the cap, is grok's rate reservation missing the first tile-part's SOT and SOD, fixed separately in grok
- **Closed captions were in the wrong namespace**: a SMPTE closed caption is now written `<tt:ClosedCaption xmlns:tt="http://www.smpte-ra.org/schemas/429-12/2008/TT">` with a `<tt:Language>`, which is what ST 429-12 declares and what the ISDCF Bv2.1 test DCP and libdcp both write. It used to be a `<MainClosedCaption>` in the CPL's own 429-7 namespace, where the reel AssetList takes nothing after MainSubtitle but a foreign-namespace element, so every captioned package we built carried a schema violation. The other children come from 429-7's TrackFileAssetType and are unchanged. Readers here take either spelling with or without a prefix
- **`decrypt` dropped the Atmos track and refused encrypted timed text**: since `--encrypt` started keying the subtitle, closed-caption and Atmos essence, an encrypted package carrying any of them could not be turned back into cleartext. `decrypt` now reads an encrypted timed-text MXF as its document plus every ancillary resource and rewraps it with the same asset id, document, duration and resource ids, and reads an Atmos MXF frame by frame and rewraps that with its own asset id, with the reel carrying the AuxData block back. The Atmos track used to be left out of the rebuilt reel entirely, encrypted or not, so a decrypted package silently lost it. A track whose KeyId no key covers fails loud naming the asset. `transcode-dcp` carries the same three tracks through the same two functions
- **One 23.976-to-24 conform for both front ends**: a 24000/1001 source packaged at 24 fps is read 1:1 and plays 0.1% faster, with the sound pulled up by the same 1000/1001, which is what DCP-o-matic does by default. `hfr::conform_source_to_dcp` is the one place that decides it and `hfr::audio_pull_up` the one place that runs it. The GUI used to hand the source to an `fps=24` filter with no input rate, so ffmpeg duplicated one frame in every 1001 and kept the running time, and it never pulled the sound up at all. `pipeline` did both of those and had no pull-up either. Both now set postkit's `read_source_at`, which reaches ffmpeg as an input `-r` before `-i`, so every source frame is packaged once. `create` already read the picture 1:1 and pulled the sound up; its inline ffmpeg step is now that shared function. The hints say what will happen before the build, in `create --check` and the GUI's dialog
- **The subtitle options stopped at the create path**: `create --versions`, `create-multi`, `create-vf` and every closed-caption track rendered their timed text under the default options, so `--subtitle-font` and the placement, RTL and wrap flags were inert there and a machine with no system sans face could not build any of them at all. All four now render with the options the job was given, `create-vf` takes a `--subtitle-font` of its own, and a caption track carries the font and the placement while keeping the default appearance, which is what `--ccap` is documented to do. The eight end-to-end tests that skipped themselves on a machine with no font now name the face in the repo and run everywhere
- **Reels without cues carried no subtitle**: `create --reel-length` and `create
  --versions` gave a reel none of the cues fell into no MainSubtitle at all, which
  dcpdoctor reports as `subtitle_missing_from_reel` because a composition with
  subtitles needs one on every reel. Every reel now carries a timed-text asset
  spanning it, and a reel with nothing to show carries the placeholder cue
  DCP-o-matic writes for the same case: a single space from the reel start, one
  second long, with the same embedded font and `LoadFont` as any text cue. ST
  428-7 has no document without a cue in it, which is why the placeholder is a cue
  rather than an empty `SubtitleList`. The wrap takes the reel's frame count as
  the essence duration instead of deriving it from the last cue, so
  IntrinsicDuration and Duration match the picture on every reel. Closed captions
  follow the same rule, since every reel has to carry the same number of them
- **Timed-text packaging was not conformant**: every package carrying a converted
  subtitle or caption track failed six of dcpdoctor's ST 429-5 / ST 428-7 checks, and
  libdcp's verifier reads the same rules. The MXF ResourceID was the track file's own
  asset id, where ST 429-5 wants the document's `<Id>` and wants the file, the
  document and the resource to be three different things. The document declared no
  `StartTime`, so a player had nothing saying the reel starts at zero. A `Font` named
  `font1` that no `LoadFont` introduced, because a track built without
  `--subtitle-font` embedded no font at all. The `IssueDate` carried a `+00:00`
  suffix Deluxe QC rejects, the root declared a second, unused namespace, and the
  reel's MainSubtitle carried no `<Hash>`, which some servers check instead of the
  PKL's. A text track now always embeds a font, the machine's sans-serif face when
  the caller names none, and refuses the build when the machine has none rather than
  packaging text nothing can draw. Rebuild any package with a subtitle or caption
  track
- **A failed `create` exited 0** — eighteen fatal errors logged and returned without a failing exit code, so a watch folder or script read the run as a success and shipped nothing. They now exit non-zero. `--container` is also resolved before the encode rather than after it, so a bad value fails in a second instead of after a full J2K pass
- **`create-multi` carried its own copy of the container table** — it skipped the dimension validation `create` applies and could drift from it. Both now resolve the container through one function
- **`decrypt` and `transcode-dcp` dropped a package's markers** — both re-author the CPL around the same composition, and the marker track was not carried over, so a marked package came out unmarked. Markers are now read back from the source CPL and rewritten, offsets unchanged
- **The CPL declared a geometry the essence did not have** — picture geometry came from the `--container` value or the 2K/4K preset, so an ordinary 1998x1080 flat master with no container flag was packaged with a CPL claiming 2048x1080. Every declaration now comes from the coded raster read back from the codestream: `MainPictureStoredArea` and `ScreenAspectRatio` describe the frames, `MainPictureActiveArea` carries the container (what a projector masks to), and a container larger than the frames is refused instead of declaring an active area the picture does not have
- **Encrypted DCPs shipped cleartext subtitles and Atmos** — picture and sound were encrypted at wrap time while the timed-text and Atmos tracks went out in the clear, with nothing in the package saying so. `--encrypt` now refuses a package carrying either track. Encrypting them needs the content key threaded through postkit's timed-text and Atmos wraps
- **`--twok` / `--fourk` misframed a source of another size** — the flags overwrote the encode raster with the container size while ffmpeg kept decoding at the source raster, so every frame of e.g. a 1998x1080 flat master was read misaligned and the CPL declared a size the J2K frames did not have. The mismatch is now refused up front, naming both rasters. Fitting the source automatically is still to come; use `convert` meanwhile
- **3D encoded at twice the requested bit rate** — `create --right-eye` encoded both eyes at the full `--video-bit-rate`, so the delivered stereoscopic track ran at double the requested rate (500 Mbps for `--video-bit-rate 250`) and could breach the DCI cap. The ceiling covers the whole stereoscopic track, so each eye now gets half the budget, as libdcp does

### Added
- **Rebindable GUI keyboard shortcuts**: every shortcut now lives in one registry. Ctrl+K opens an overlay listing them by category, where clicking a shortcut captures a new key combination (Backspace or Delete unbinds, Escape cancels, a combination already in use is refused and names its owner). Per-shortcut and global reset buttons restore the defaults, and changes persist across restarts
- **KDM distribution** — cinema/screen database (`cinema add|list|remove|add-screen|remove-screen|search|import-flm`, no private keys stored), named validity templates (`kdm-template`, used via `kdm/kdm-batch --template`), generation history (`kdm-history`, metadata-only JSONL), FLM-x / SMPTE 430-7 import (`cinema import-flm`), email delivery (`kdm/kdm-batch --email-to --smtp-config`, one zipped email per cinema), and `kdm-batch --cinema/--screen/--cert-dir` recipient resolution. `cert-fetch --vendor --serial` downloads recipient certs from the credential-free public endpoints (dolby/doremi, qube)
- **`decrypt` subcommand** — turn an encrypted DCP into a cleartext DCP of the same reel structure. Content keys come from a KDM + recipient RSA key (`--kdm --recipient-key`) or the DCP's `KEYS.json` (`--keys`); a wrong key or an uncovered KeyId fails loud. Key material stays in memory only
- **`assemble` subcommand** — build one new OV composition (single CPL) whose reels are the input DCPs' reels in program order; essence is copied byte-identical and referenced by its existing UUIDs. Inputs must share standard/rate/resolution and must not be encrypted
- **`edit` subcommand** — rewrite a DCP's CPL metadata (title/annotation/content-kind/issuer) without re-wrapping essence, assigning a new CPL id and refreshing PKL/ASSETMAP atomically. Refuses encrypted DCPs
- **`create-multi` subcommand** — multi-composition package: one CPL per manifest entry, each with its own picture/sound/subtitle, over one shared PKL/ASSETMAP (contrast `create --versions`, several CPLs over shared essence)
- **Subtitle VF** — `create-vf --replace-subtitle`/`--add-subtitle REEL=PATH` (with `--subtitle-language`) wraps an SRT or SMPTE XML into a per-reel timed-text MXF; a subtitle-only VF references the OV picture/sound by id and ships only the new subtitle MXF
- **Explicit reel splits** — `create --split-at <tc>[,...]` (HH:MM:SS or HH:MM:SS:FF) and `create/pipeline --split-chapters` (source chapter marks via ffprobe) feed the same multi-reel path as `--reel-length`, rejecting any reel shorter than 1 second
- **Custom container** — `create --container <2k-scope|...>` and `create --container-dims WxH` (even, within the 2K/4K bound; threads into CPL ScreenAspectRatio and MainPicture stored/active area)
- **Head/tail padding** — `create --pad-head <dur> --pad-tail <dur>` (`48f`/`2s`) with sample-accurate silence; `--pad-color <RRGGBB>` fills pad frames with an sRGB colour (routed through the DCI X'Y'Z' transform) instead of black. Rejected loud when combined with reel-length/3D/Atmos or head-padding a supplied SMPTE subtitle XML
- **Input decode range** — `create/pipeline --input-range full|legal` forces the source's decode range, correcting wrong or absent range flags (re-decodes into a lossless FFV1 intermediate)
- **Disk writer** — `format-drive <target> --fs ext2|ext3 [--label] --yes [--image]` (refuses mounted targets) and `check-drive <target>` (report fs type + label, works unprivileged on image files)
- **Sign-language video** — `create --sign-language-video <file> --sign-language-lang <rfc5646>` (ISDCF Doc 13): VP9-packed into 288 KB PCM blocks on sound channel 15, labeled SLVS via MCA, with the ISDCF SignLanguageVideo ExtensionMetadata in the CPL
- **Create-time audio processing** — `create --upmix a|b` (stereo→5.1), `--loudness-target leqm=<db>|lufs=<v>` with `--true-peak-ceiling`, and filename channel auto-routing (a directory of mono `name_L.wav`/`_R`/... files). Standalone `crossfade --a --b -o --overlap` (equal-power) and `mid-side-decode -i -o --mid --side`
- **Encode QoL on `create`** — `--start-at HH:MM|RFC3339|+offset` (scheduled wall-clock start), `--resume` (reuse on-disk J2K frames after an interruption, survives restarts), average-fps ETA in progress output, `--shutdown-when-done` (power off after a clean encode)
- **Subtitle input formats + authoring** — `create --subtitle` accepts `.ass`/`.ssa`, `.pac`, `.mks`/`.mkv`, `.fcpxml` and Interop `DCSubtitle` XML with PNG bitmap subs (styling and per-cue alignment carried into the DCST). Placement (`--subtitle-halign/--subtitle-valign/--subtitle-vposition`), 3D depth (`--subtitle-zposition`), RTL (`--subtitle-rtl auto|on|off`), auto-wrap (`--subtitle-wrap <chars>`) and font embedding + subsetting (`--subtitle-font`, `--subtitle-no-subset`, cross-reel font dedup)
- **`subtitle-edit` subcommand** — edit a standalone subtitle file (any parsable format): `--list`, `--shift-ms`, or `--index N` with `--text`/`--set-start-ms`+`--set-end-ms`, written back as SRT
- **HDR source delivery** — `create --hdr-to-dci-lut <lut>` runs the LUT before J2K encode; `--allow-generic-hdr-tonemap` opts into FFmpeg tone mapping with a warning. APV source files decode through ffmpeg when the local build has the decoder
- **`subtitle-extract` subcommand** — export timed text from a DCP (or a bare subtitle asset) back to `.srt` (timing preserved) or `.txt` (text only), format chosen by output extension. Reads MXF-wrapped ST 428-7 resources via asdcplib and loose SMPTE/Interop XML, resolves subtitle assets through the CPL/ASSETMAP, and concatenates reels with their timeline offsets applied. GUI extraction panel added
- **Destination free-space check (DoM 3150)** — `create` and `copy` now fail early with a clear message when the required bytes (essence for create, source total for copy) exceed the free space on the destination filesystem, instead of running out mid-write. Cross-platform via `statvfs` (Unix) and `GetDiskFreeSpaceExW` (Windows)
- **CompositionMetadataAsset (Bv2.1)** — SMPTE CPLs now carry a ST 429-16 `CompositionMetadataAsset` in the first reel with `MainSoundConfiguration` (e.g. `51/L,R,C,LFE,Ls,Rs` with `-` padding for the silent fill channels), `MainSoundSampleRate`, `MainPictureStoredArea`/`ActiveArea`, `FullContentTitleText` and the Bv2.1 `ExtensionMetadata` marker. The sound configuration is derived from the packaged channel count, so validators no longer flag "0 channels but sound assets have N". Interop CPLs are unchanged. Verified: the asset validates against the ST 429-16 XSD and the whole CPL against ST 429-7 via xmllint
- **`ingest-package` subcommand** — metadata-only repackaging: scans a directory, reads each MXF's embedded asset UUID (asdcplib), and regenerates ASSETMAP/PKL/VOLINDEX covering every asset file present, reusing hashes from the old PKL where available. Fixes exported OV/VF folders (e.g. a Sony server's VF export) whose ASSETMAP/PKL omit hardlinked assets referenced by the CPL. Essence passes through untouched; the old ASSETMAP/PKL are replaced. Both SMPTE and Interop naming
- **Configurable subtitle position** — `subtitle-convert --vposition <percent>` (default 8) sets the bottom line's distance from the bottom of the screen
- **Stereoscopic 3D** — `create --right-eye <input>` (main input is the left eye) encodes both eyes at the same settings, wraps them into one ST 429-10 stereoscopic picture MXF (postkit `wrap_stereoscopic`) and emits the CPL `MainStereoscopicPicture` element (429-10/2008 namespace, EditRate at the composition rate, FrameRate doubled for interleaved L/R). Eye frame counts must match. Verified: a short 3D DCP passes `dcpdoctor validate` with 0 errors and the stereoscopic element validates against the clairmeta 429-10 XSD via xmllint
- **MCA channel labeling** — every PCM sound wrap now carries ST 429-12 / 377-4 MCA labels derived from the probed channel count (2.0 / 5.1 / 7.1). `create --hi-channel <n>` / `--vi-channel <n>` label a channel index as the standalone HI / VI-N accessibility track. Verified by reading the labels back out of the MXF with asdcplib (`mca_labels`)
- **Dolby Atmos aux track** — `create --atmos <file-or-dir>` wraps a Dolby Atmos / DCData bitstream (postkit `EssenceType::Atmos`) and registers it as a ST 429-18 `AuxData` element (Dolby `2012/AD` namespace, IAB data-essence UL) in the CPL/PKL/ASSETMAP. Verified: an Atmos DCP with a synthetic payload passes `dcpdoctor validate`; real-essence conformance needs real Atmos material
- **Interop KDM** — `kdm --format smpte|interop` (default smpte) and `kdm-batch --format`. Interop uses postkit's digicine ETM (134-byte key block). Verified: an Interop KDM carries the digicine namespace and verifies with xmlsec1. Legacy output, mark for real-gear validation before production use
- **`transcode-dcp` subcommand** — re-encode an existing DCP's picture essence to a lower bandwidth (`--video-bit-rate`, optional `--width`/`--height` rescale). Extracts each J2K frame from the picture MXF (asdcplib), decodes with grk_decompress, re-encodes with grok at the target ratio, rewraps, and emits a fresh CPL/PKL/ASSETMAP with correct hashes. Audio and subtitle tracks are copied unchanged; encrypted input is rejected
- **Reel splitting**: `create --reel-length <minutes>` splits the picture, audio (sample-accurate at the frame edge) and subtitles (DCST re-split and rebased per reel) into one MXF set per reel and emits a multi-reel CPL over a shared PKL/ASSETMAP; every reel is at least 1 second, a sub-second remainder merges into the previous reel. Absent the flag the single-reel path is unchanged
- **HFR validation**: legal DCP rates are 24/25/30/48/50/60/96/100/120 (SMPTE; Interop stays 24/25/30/48); 4K is capped at 30 fps. Illegal fps/resolution combos are rejected loudly before encoding; the GUI frame-rate menus offer exactly the legal set
- **Subtitle packaging** — `create --subtitle <srt> [--subtitle-language]` converts SRT to ST 428-7 DCST XML, wraps it as a timed-text MXF and registers the track in the reel/CPL/PKL/ASSETMAP
- **`export` subcommand** — ProRes/H.264/H.265/DNxHR/image-sequence export of a picture MXF (previously only reachable via the batch daemon)
- **`transcode --format` / `--bit-depth`** — wired the GUI transcode controls to the real backend
- **`watch --webhook-url`** — POST a JSON notification when a new DCP is detected

### Changed
- **The still hold, crop resolution, free-space check and kdm store helpers come from postkit**: `still` and the auto-crop/fill-crop/conflict logic of `source_picture` were near-copies of imfwizard's, drifted apart, and both now delegate to `postkit::still` and `postkit::picture_processing`. `free_space` moved to `postkit::free_space` unchanged, and the kdm store's atomic write and untrusted-cert parsing are `postkit::fs::write_atomic` and `postkit::certificate` now, leaving `store` as just the data-dir paths. Three behaviour changes ride along: passing two of `--crop-*`/`--auto-crop`/`--fill-crop` says "give only one of them", naming the flags, instead of "one or the other"; `--auto-crop` refuses a threshold outside 0..1 and a source that is all black at the threshold instead of cropping to nothing; and `--rotate`/`--flip` accept the union of the two wizards' spellings, adding `0` for no rotation.
- **`edit` is postkit's package edit**: the rewrite moved to `postkit::package_edit::edit_package`, which does the same job for an IMF composition, so a DCP and an IMP are retitled by one piece of code instead of two that can drift. `edit`'s flags, exit code and log lines are unchanged, as is the GUI's retitle. Four things now behave differently. Asking for a field the CPL does not carry is an error instead of a silent no-op, so `--issuer` on a CPL with no `<Issuer>` says so rather than reporting success over an unchanged document. A CPL that cannot be hashed is an error instead of a PKL entry written with an empty `<Hash>`. An ASSETMAP that does not list the composition is an error instead of a rewrite that leaves the package inconsistent. And `--output` copies subdirectories, where it used to copy only the files at the top of the package and quietly drop anything nested.
- **TMS upload lives in postkit**: the config type, the `TmsTransport` trait, both transports and the upload itself moved to `postkit::tms` behind a feature, so imfwizard can deliver an IMP the same way. Nothing about `tms` or `create --upload-to-tms` changes: the config file is still `~/.config/dcpwizard/tms.toml` unless `--tms-config` points elsewhere, and reading it stays here because the path carries this app's name.
- **Encode bandwidth** — the GUI now maps the target Mbps to the J2K compression ratio (was hardcoded)
- **`colour --target xyz`** — routes through the real DCDM Rec.709/P3/Rec.2020 → X'Y'Z' transform (fails loud on an unsupported source)
- **PCM wrap** — rejects non-DCP audio sample rates (48/96 kHz only) instead of mislabeling
- **KDM validity** — duration-based end times keep the start's UTC offset
- **GUI resolution** — scope/flat/full map to the correct container dimensions; content kind `episode` maps to a real ContentKind
- **Subtitle timecodes** — SMPTE DCST now emits frame-based `HH:MM:SS:FF` (schema-valid), not `HH:MM:SS.mmm`
- **Shared GUI frontend**: the preview player, keyboard shortcuts and base stylesheet now come from the PostPerfection/guikit submodule at `extern/guikit` instead of per-app copies, shared with imfwizard. `gui/src/style.css` keeps only the dcpwizard-specific rules, loaded after the base. Building the GUI from source now requires the submodules checked out (`git clone --recurse-submodules`, or `git submodule update --init`). No behaviour change

### Removed
- **DTS:X** — was silently mapped to the Atmos (IAB) essence UL, which is wrong for DTS:X. Removed the `MxfType::DtsX` variant and dropped DTS:X from the docs; blocked on a confirmed DataEssenceCoding UL (see DESIGN_TODO)
- **Dead scaffolding modules** — atmos.rs, audio.rs, stereo3d.rs, dtsx.rs (shelled out to a nonexistent `asdcp-wrap`, zero callers; superseded by the real postkit-based 3D/Atmos/MCA wiring)
- **OpenJPEG encoder** — grok is now the only J2K encoder. Dropped the `create --encoder` flag and the openjpeg branch; the postkit dependency enables `grok-ffi` (not `openjpeg`). libgrokj2k must be discoverable by pkg-config at build time and loadable at runtime
- **KDM `--formulation`** — was inert (MessageType is fixed by ST 430-1); removed from CLI/config/docs
- **`verify --output .pdf`** — silently produced plain text; option dropped
- **Dead modules** — dcp_diff, plugin, preferences, geometry, and the prores re-export shim (zero callers)

### Added
- **Signing on the rest of the packaging commands** — `assemble`, `create-vf`, `create-multi`, `combine` and `ingest-package` take `--signer-cert/--signer-key/--signer-chain` like `create` does. Each signs a CPL before anything hashes it into the packing list. `combine` and `ingest-package` sign only the PKL they generate, since they hash the CPLs rather than rewriting them, so a signature a CPL already carries stays valid. `create-vf` signs after the supplemental marker is written, not before, or the marker would invalidate it. `conform` takes them too, and signs whichever package it delivers: the per-reel DCP when a single reel is moved straight out, the assembled CPL and PKL when it merges reels

### Fixed
- **Signer certificates were missing Basic Constraints and Key Usage** (via postkit) — every chain from `certificate chain` had a bare leaf, which ST 430-2 requires to carry both, so validators rejected packages signed with it. Regenerate any chain generated earlier
- **`edit` no longer leaves a stale signature** — editing a signed DCP rewrote the CPL, PKL and ASSETMAP but kept their `ds:Signature`, so the output verified as tampered (dcpdoctor `signature_invalid`). `edit` now drops the signature from every document it rewrites, and warns which ones lost it. The package comes out unsigned rather than falsely signed. Re-sign it afterwards if it has to stay signed
- **Subtitle vertical position** — SMPTE and Interop subtitle generators anchored the block at the top (Vposition 85 with Valign="bottom") so subtitles rendered near the top of the screen. The bottom line now sits at 8% from the bottom with lines stacked upward (a two-line cue renders at 15% and 8%)
- **GUI "Show in Files"** — uses the tauri opener plugin (`revealItemInDir`); the shell `open` call only accepted URLs
- **Copy-to-drive verify** — flushes to the device and drops the page cache before reading back, so verification is real
- **FFOC marker value** — default markers now emit FFOC = 1 (was 0). libdcp's Bv2.1 verifier raises `INCORRECT_FFOC` unless FFOC == 1; LFOC stays at the last frame

## [1.1.0] — 2026-05-28

### Added
- **Timeline view** — Visual DCP timeline with multi-reel navigation, duration bars, and asset type indicators
- **CLI flag consistency test** — `tests/cli_flags_test.sh` verifies GUI invocations match actual CLI flags
- **CI: CLI flag check** — Automated verification in GitHub Actions pipeline

### Fixed
- **Properties panel → build pipeline** — All properties (standard, resolution, framerate, bandwidth, colour, content_kind, encrypt, stereo_3d, channels) now correctly passed to the DCP creation pipeline
- **Verify flags** — `--check-mxf` → `--no-picture-check`, `--skip-hashes` → `--no-hash-check` (correct inverted logic)
- **Encode flags** — Removed non-existent `--resolution`/`--framerate`, added `-i` input flag
- **Transcode flags** — Removed non-existent `--format`/`--bit-depth`, uses `-i` flag
- **Copy flags** — Positional args replaced with `--src`/`--dst` named args
- **Report flags** — Uses `--dcp` and `-o` instead of non-existent `--format`
- **Subtitle conversion** — `Command.create` → `Command.sidecar`, `--framerate` → `--fps`, added `-i`/`-l` flags
- **Burn-in** — `Command.create` → `Command.sidecar`, correct `-i`/`-s`/`-o` flags
- **Target conversion** — `Command.create` → `Command.sidecar`, correct `-i`/`-t`/`-m` flags
- **KDM** — Added CPL ID, content title, and output path fields; uses correct `--cpl-id`/`--content-title`/`--cert`/`-o` flags
- **Encrypt standalone** — Shows informational message (standalone re-encryption not supported by CLI)

## [1.0.0] — 2025-01-20

### Added
- **CLI: Create subcommand** — Full DCP creation from JPEG 2000 + audio MXF inputs
  - `--content-type` (feature, trailer, advertisement, etc.)
  - `--frame-rate` (24, 25, 30, 48, 60)
  - `--twok` / `--fourk` resolution selection
  - `--video-bit-rate` for encoding control
  - `--threads` for parallel encoding
  - `--container-ratio` (flat, scope, full)
  - `--audio-channels` configuration
- **CLI: Verify subcommand** — Full DCP verification with options
  - `--no-hash-check` to skip hash verification
  - `--no-picture-check` to skip picture essence inspection
  - `--strict` for SMPTE-strict mode
  - `--output` for writing reports to file (text or HTML)
  - `--quiet` to suppress progress output
- **CLI: KDM subcommand** — Key Delivery Message generation
  - `--valid-from` / `--valid-to` for validity periods (`--valid-to` accepts a
    relative duration: "2 weeks", "30 days", "4 hours")
  - `--keys` to carry the DCP's content keys (from `create --encrypt`)
  - `--signer-cert` / `--signer-key` (required) for the ETM signature
  - `--formulation` (modified-transitional-1, dci-any, dci-specific)
- **Panic hook** — User-friendly crash messages with issue tracker link
- **CLI integration tests** — 10 end-to-end tests using assert_cmd
- **Release CI** — GitHub Actions workflow for building release binaries on tag push
- **GUI Release CI** — Tauri build workflow producing .deb, .AppImage, .dmg, .msi

### Changed
- Version unified to 0.5.0 across all workspace crates
- Git dependencies pinned to v0.5.0 tags (asdcplib-rs, dcpdoctor, postkit)
- ContentType, Resolution, Standard enums now in dcpwizard-core::lib

### Fixed
- OpenJPEG thread-safety issues resolved via Grok JPEG 2000 encoder
- Clippy warnings cleaned up across entire workspace
