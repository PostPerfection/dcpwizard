[project]
name = "dcpwizard"
version = "1.0.0"
description = "Python bindings for DCP Wizard"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
