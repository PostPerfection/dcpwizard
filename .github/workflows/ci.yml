name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            name: Linux
          - os: macos-latest
            name: macOS
          - os: windows-2022
            name: Windows

    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}

    steps:
      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libxml2-dev \
            libxml2-utils \
            libssl-dev \
            libxerces-c-dev \
            xmlsec1 \
            fonts-dejavu-core \
            e2fsprogs

      # apt's 6.1 and chocolatey's have no apv decoder, so linux and windows take
      # the same pinned 8.1 static build
      - name: Install ffmpeg (Linux)
        if: runner.os == 'Linux'
        run: |
          curl -fsSL --retry 5 --retry-all-errors -o ffmpeg.tar.xz https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.1-latest-linux64-gpl-8.1.tar.xz
          tar -xJf ffmpeg.tar.xz
          echo "$PWD/$(ls -d ffmpeg-*linux64-gpl-8.1)/bin" >> "$GITHUB_PATH"

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install pkg-config libxml2 openssl@3 xerces-c libxmlsec1
          OPENSSL_PREFIX=$(brew --prefix openssl@3)
          echo "OPENSSL_DIR=$OPENSSL_PREFIX" >> $GITHUB_ENV
          echo "OPENSSL_LIB_DIR=$OPENSSL_PREFIX/lib" >> $GITHUB_ENV
          echo "OPENSSL_INCLUDE_DIR=$OPENSSL_PREFIX/include" >> $GITHUB_ENV
          echo "LIBRARY_PATH=$OPENSSL_PREFIX/lib:$(brew --prefix xerces-c)/lib" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=$OPENSSL_PREFIX/lib/pkgconfig:$(brew --prefix libxml2)/lib/pkgconfig:$(brew --prefix xerces-c)/lib/pkgconfig" >> $GITHUB_ENV

      - name: Install ffmpeg (macOS)
        if: runner.os == 'macOS'
        shell: bash
        # conda-forge and homebrew build ffmpeg without libzimg, and the hdr decode chain needs zscale
        run: |
          base=https://ffmpeg.martin-riedl.de/download/macos/arm64/1787073674_9.0.1
          mkdir -p "$HOME/ffmpeg-bin"
          for tool in ffmpeg ffprobe; do
            curl -fsSL --retry 5 --retry-all-errors -o "$tool.zip" "$base/$tool.zip"
            unzip -q -o "$tool.zip" -d "$HOME/ffmpeg-bin"
            chmod +x "$HOME/ffmpeg-bin/$tool"
          done
          shasum -a 256 -c <<'SUMS'
          8287a1b2229e05eb41859f073e18e6c52c60a778f2f5e6881070fe51b79407fe  ffmpeg.zip
          102a26b8940a053298d9929bfaae71e4b6ef65ba5f19a99a88c433108560741a  ffprobe.zip
          SUMS
          echo "$HOME/ffmpeg-bin" >> "$GITHUB_PATH"

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          $env:VCPKG_BUILD_TYPE="release"
          # vcpkg reads curl 56 and 503 off github's archive host as permanent and
          # refuses to retry the source download, so retry the whole install
          for ($attempt = 1; $attempt -le 5; $attempt++) {
            vcpkg install libxml2[tools] openssl xerces-c --triplet x64-windows
            if ($LASTEXITCODE -eq 0) { break }
            if ($attempt -eq 5) { throw "vcpkg install failed after $attempt attempts" }
            Start-Sleep -Seconds (30 * $attempt)
          }
          echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" >> $env:GITHUB_ENV
          echo "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\tools\libxml2" >> $env:GITHUB_PATH

      - name: Install ffmpeg (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          curl -fsSL --retry 5 --retry-all-errors -o ffmpeg.zip https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.1-latest-win64-gpl-8.1.zip
          unzip -q ffmpeg.zip
          echo "$(cygpath -w "$PWD/$(ls -d ffmpeg-*win64-gpl-8.1)/bin")" >> "$GITHUB_PATH"

      - name: Install xmlsec1 (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # a slow msys2 mirror fails the sync outright, so retry the install
          for attempt in 1 2 3 4 5; do
            /c/msys64/usr/bin/pacman.exe -Sy --noconfirm --needed mingw-w64-x86_64-xmlsec && break
            if [ "$attempt" -eq 5 ]; then
              echo "pacman install failed after $attempt attempts"
              exit 1
            fi
            sleep $((30 * attempt))
          done
          echo "C:\msys64\mingw64\bin" >> "$GITHUB_PATH"

      - name: Install dovi_tool and hdr10plus_tool (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          mkdir -p "$HOME/hdr-tools"
          curl -fsSL --retry 5 --retry-all-errors -o dovi_tool.tar.gz https://github.com/quietvoid/dovi_tool/releases/download/2.3.3/dovi_tool-2.3.3-x86_64-unknown-linux-musl.tar.gz
          curl -fsSL --retry 5 --retry-all-errors -o hdr10plus_tool.tar.gz https://github.com/quietvoid/hdr10plus_tool/releases/download/1.7.2/hdr10plus_tool-1.7.2-x86_64-unknown-linux-musl.tar.gz
          tar -xzf dovi_tool.tar.gz -C "$HOME/hdr-tools"
          tar -xzf hdr10plus_tool.tar.gz -C "$HOME/hdr-tools"
          chmod +x "$HOME/hdr-tools/dovi_tool" "$HOME/hdr-tools/hdr10plus_tool"
          echo "$HOME/hdr-tools" >> "$GITHUB_PATH"

      - name: Install dovi_tool and hdr10plus_tool (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          mkdir -p "$HOME/hdr-tools"
          curl -fsSL --retry 5 --retry-all-errors -o dovi_tool.zip https://github.com/quietvoid/dovi_tool/releases/download/2.3.3/dovi_tool-2.3.3-universal-macOS.zip
          curl -fsSL --retry 5 --retry-all-errors -o hdr10plus_tool.zip https://github.com/quietvoid/hdr10plus_tool/releases/download/1.7.2/hdr10plus_tool-1.7.2-universal-macOS.zip
          unzip -q -o dovi_tool.zip -d "$HOME/hdr-tools"
          unzip -q -o hdr10plus_tool.zip -d "$HOME/hdr-tools"
          chmod +x "$HOME/hdr-tools/dovi_tool" "$HOME/hdr-tools/hdr10plus_tool"
          echo "$HOME/hdr-tools" >> "$GITHUB_PATH"

      - name: Install dovi_tool and hdr10plus_tool (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          curl -fsSL --retry 5 --retry-all-errors -o dovi_tool.zip https://github.com/quietvoid/dovi_tool/releases/download/2.3.3/dovi_tool-2.3.3-x86_64-pc-windows-msvc.zip
          curl -fsSL --retry 5 --retry-all-errors -o hdr10plus_tool.zip https://github.com/quietvoid/hdr10plus_tool/releases/download/1.7.2/hdr10plus_tool-1.7.2-x86_64-pc-windows-msvc.zip
          unzip -q -o dovi_tool.zip -d hdr-tools
          unzip -q -o hdr10plus_tool.zip -d hdr-tools
          echo "$(cygpath -w "$PWD/hdr-tools")" >> "$GITHUB_PATH"

      - name: Setup grok
        uses: PostPerfection/setup-grok@v1
        with:
          grok-ref: v20.4.6

      - name: Check test tools
        shell: bash
        run: |
          ffmpeg -version | head -n 1
          ffprobe -version | head -n 1
          xmllint --version
          xmlsec1 --version
          dovi_tool --version
          hdr10plus_tool --version
          if [ "$RUNNER_OS" = "Linux" ]; then mkfs.ext2 -V; fi

      - name: Build
        working-directory: rust
        run: cargo build --release

      - name: Test
        if: runner.os != 'macOS'
        working-directory: rust
        run: cargo test --workspace --no-fail-fast

      # the macos run has hung inside a test more than once, so after the
      # deadline every test binary still running gets its stacks sampled
      - name: Test (macOS, with a hang watchdog)
        if: runner.os == 'macOS'
        working-directory: rust
        run: |
          cargo test --workspace --no-fail-fast &
          tests=$!
          (
            sleep 2400
            echo "::group::tests still running after 40 minutes, sampling"
            for pid in $(pgrep -f 'target/debug/deps/'); do
              ps -p "$pid" -o pid=,command=
              sample "$pid" 3 2>/dev/null | head -200
            done
            echo "::endgroup::"
            kill "$tests"
          ) &
          watchdog=$!
          wait "$tests"
          status=$?
          kill "$watchdog" 2>/dev/null || true
          exit "$status"

      - name: Clippy
        working-directory: rust
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Format check
        working-directory: rust
        run: cargo fmt --all -- --check

      - name: CLI flag consistency
        if: runner.os == 'Linux'
        run: bash tests/cli_flags_test.sh ./rust/target/release/dcpwizard

      - uses: astral-sh/setup-uv@v6

      - name: CLI tests
        shell: bash
        run: PATH="$PWD/rust/target/release:$PATH" uv run --no-project --with pytest pytest tests/test_cli.py -q

  gui:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: dtolnay/rust-toolchain@stable

      - uses: pnpm/action-setup@8912a9102ac27614460f54aedde9e1e7f9aec20d # v6.0.5
        with:
          version: 11.8.0

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: gui/pnpm-lock.yaml

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libxml2-dev libssl-dev libxerces-c-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev xvfb xdotool

      - name: Setup grok
        uses: PostPerfection/setup-grok@v1
        with:
          grok-ref: v20.4.6

      - uses: PostPerfection/setup-libmpv@v1
        with:
          mpv-release: 20260828
          mpv-archive: mpv-dev-x86_64-20260828-git-182fa6ca49.7z

      - name: Build frontend
        working-directory: gui
        run: |
          pnpm install --frozen-lockfile
          pnpm build

      - name: Build CLI sidecar
        working-directory: rust
        run: |
          cargo build --release -p dcpwizard-cli
          TRIPLE=$(rustc -vV | grep host | awk '{print $2}')
          cp target/release/dcpwizard ../gui/src-tauri/dcpwizard-${TRIPLE}

      - name: Test Tauri backend
        working-directory: gui/src-tauri
        run: cargo test

      - name: Clippy Tauri backend
        working-directory: gui/src-tauri
        run: cargo clippy --all-targets -- -D warnings

      - name: Format check Tauri backend
        working-directory: gui/src-tauri
        run: cargo fmt -- --check

      - name: Build the GUI
        # without custom-protocol tauri builds a dev binary that loads the vite
        # dev url instead of the embedded frontend, tauri build turns it on
        run: cargo build --release --manifest-path gui/src-tauri/Cargo.toml --features tauri/custom-protocol

      - uses: astral-sh/setup-uv@v6

      - name: GUI launch test
        run: |
          xvfb-run -a --server-args="-screen 0 1280x1024x24" \
            uv run --no-project --with pytest --with python-xlib pytest tests/test_gui.py -q
